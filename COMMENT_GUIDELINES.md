# 项目注释规范

> 本规范适用于 MetaBlog 项目所有 TypeScript/JavaScript/Vue 文件. 

---

## 原则

1. **注释服务于 LLM 和人类开发者**,不是装饰. 
2. **必要处必须详细** — 复杂算法、边界情况、hack 原因、设计决策. 
3. **禁止无意义注释** — `// 定义变量 a` 这种代码自解释的内容不需要注释. 
4. **只加注释,不改代码** — 本次任务不修改任何功能逻辑. 

---

## 文件头注释(必须)

每个 `.ts/.js/.vue` 文件顶部必须包含：

```typescript
/**
 * ============================================================================
 * <文件用途一句话说明>
 * ============================================================================
 *
 * <2-3 句话详细说明本文件职责、与上下游的关系>
 *
 * @module <模块路径,如 theme/tools/lark>
 */
```

---

## 导出函数/类注释(必须)

所有 `export` 的函数、类、接口、类型别名必须使用 JSDoc：

```typescript
/**
 * <一句话功能说明>
 *
 * <详细说明(2-3 句话)：使用场景、设计意图、与相关函数的关系>
 *
 * @param name - <参数说明：类型、约束、默认值、示例值>
 * @param options - <复杂对象参数需逐字段说明>
 * @returns <返回值说明：类型、格式、特殊值含义>
 *
 * @example
 * ```typescript
 * <具体调用示例>
 * ```
 *
 * @remarks
 * <注意事项、边界情况、常见错误、hack 原因>
 *
 * @throws <可能抛出的异常及触发条件>
 */
```

---

## 复杂逻辑注释(必须)

当代码包含以下情况时,必须在代码块上方或旁边添加注释：

| 场景 | 注释要求 |
|------|----------|
| 非显而易见的算法 | 说明算法原理和选择理由 |
| 边界情况处理 | 说明在什么边界条件下需要特殊处理 |
| Hack/Workaround | 详细说明为什么需要 hack、对应的 issue/PR、何时可以移除 |
| 性能优化 | 说明优化手段和 benchmark 结果 |
| 异步/并发逻辑 | 说明执行顺序、竞态条件处理 |
| 正则表达式 | 说明匹配的意图和每个捕获组的含义 |
| 魔数/魔法字符串 | 说明来源和含义,或改为命名常量 |
| 条件分支多且复杂 | 每个分支说明判断逻辑和预期行为 |

**格式**：
```typescript
// <为什么这样写,而不是做了什么>
// <如果有外部参考,附上链接或 issue 编号>
const result = complexOperation()
```

---

## 常量与配置注释(必须)

```typescript
/** API 请求超时时间(毫秒). 超过此时间将自动取消请求.  */
const API_TIMEOUT = 30_000

/** 单次请求最大块数. 飞书 API 限制为 50 个块/请求.  */
const MAX_BLOCKS_PER_REQUEST = 50
```

---

## 接口/类型注释(必须)

```typescript
/**
 * 飞书文档块的数据结构. 
 *
 * 每个块代表文档中的一个元素(文本、标题、代码块等). 
 * block_type 决定块的具体形态. 
 *
 * @see {@link https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/docx-v1/block/get}
 */
interface LarkBlock {
  /** 块唯一标识 */
  block_id: string
  /** 块类型：2=文本, 3=heading1, ... 14=代码块 */
  block_type: number
  /** 块内容,具体字段由 block_type 决定 */
  [key: string]: any
}
```

---

## 禁止的注释

❌ **无意义注释**：
```typescript
// 定义变量 a  ← 禁止
const a = 1

// 循环遍历数组  ← 禁止
for (const item of items) { }

// 返回结果  ← 禁止
return result
```

❌ **与代码完全重复的注释**：
```typescript
// 获取用户名称
function getUserName() { }
```

❌ **过时注释**：注释与代码行为不一致,宁可删掉也不要保留错误注释. 

---

## 注释语言

- **代码注释**：中文(面向国内开发团队)
- **JSDoc 标签**：使用标准英文标签(`@param`, `@returns`, `@example`, `@remarks`, `@throws`)
- **技术术语**：保留英文原文,如 `Promise`, `async/await`, `JSON.parse`

---

## 检查清单(Checklist)

每修改完一个文件,按此清单自查：

- [ ] 文件顶部有文件头注释？
- [ ] 所有 `export` 函数/类/接口有 JSDoc？
- [ ] JSDoc 包含 `@param` 说明每个参数？
- [ ] JSDoc 包含 `@returns` 说明返回值？
- [ ] 复杂逻辑有"为什么"的注释？
- [ ] 常量有用途说明？
- [ ] 没有无意义注释？
- [ ] 没有与代码重复的注释？
- [ ] 只加了注释,没有修改功能代码？
