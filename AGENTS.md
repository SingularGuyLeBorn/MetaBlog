# MetaBlog Agent 指令

> 本文件对项目中所有 AI 代理生效。指令优先级：用户直接对话 > 本文件 > 子目录 AGENTS.md。

---

## 强制：定期重读本文件

**Agent 必须定期重新读取 `AGENTS.md`**。

- 每完成 **3-5 个任务**后重新读取一次
- 感觉到上下文被压缩（早期记忆模糊）时立即重新读取
- 每次重新读取后简要确认已加载的约束条目

目的：防止长期会话中上下文压缩导致核心约束被遗忘。

### 为什么强制？

用户明确要求：「每一次对话你做的事情都写到一个文件内 我需要知道你在做什么 你隐藏了思考过程 我很紧张」。
零隐藏不是可选项，是必选项。

### 记录内容（必须包含）

每个任务必须按以下结构记录：

1. **问题发现** — 用户原话或报错，现象描述
2. **根因分析** — 为什么会这样，追溯到哪层代码，是否关联历史问题
3. **方案选择** — 考虑过哪些方案，列 pros/cons，**失败方案必须写**
4. **实现过程** — 改了哪些文件、关键逻辑、修改顺序
5. **修改文件清单** — 表格：文件 | 修改类型 | 说明
6. **验证结果** — 测试通过/失败，现象，日志
7. **是否复发** — 旧问题是否彻底解决，潜在复发点，关联历史日志
8. **遗留问题** — 没做完的、需要后续跟进的

### 禁止行为

❌ **不能藏**：
- "我试了一下发现不行然后改了另一种" → **两种都要记录，失败原因写清楚**
- "这个 bug 之前修过但又出现了" → **必须关联到之前的日志文件**
- "代码看起来没问题但不确定" → **记录不确定性**
- "用户没问所以我不说" → **与本次交互相关的所有操作都要记录**

### 日志文件规则

- 同一天的所有交互**追加到同一个 session 文件**，不新建 part
- 文件不加代码 diff，保持可读性（只记录改了哪些文件、关键逻辑）
- 如果当天没有已存在的 session 文件，则新建 `session-YYYY-MM-DD.md`
- 同时更新 `.dev-log/README.md` 中的「当前活跃日志」表

---

## 核心目标

**在零人类干预的情况下，自动读取项目需求，并严格按照软件工程的标准流程完成所有开发任务。**

标准流程：
```
分析 → 编码 → 测试 → 提交 → 下一个任务
```

- **分析**：理解需求、调研现状、识别风险和边界情况
- **编码**：最小改动、先读后改、遵循项目代码风格
- **测试**：改完后必须运行测试或验证功能正常，不能盲交
- **提交**：遵循 Git 纪律，写清晰的 commit message，提交前清理冗余文件
- **下一个任务**：主动识别依赖和后续工作，不等着用户催

---

## 开发原则

1. **最小改动** — 只做达成目标所需的最小修改
2. **先读后改** — 修改任何代码前，先读取相关文件理解上下文
3. **测试验证** — 改完后必须运行测试或验证功能正常
4. **文档同步** — 修改功能后，同步更新相关文档（README、guide、roadmap）
5. **Git 纪律** — 功能完成后按批次 commit，避免大量文件堆积，但也不要每个小改动都提交
6. **提交前清理** — `git add` 之前必须检查并清理冗余文件：备份文件（*.backup/*.bak）、临时测试文件（test_*.png 等）、空文件、超过 7 天的运行时日志（.logs/、.playwright-mcp/）
7. **遇阻必搜** — 编码前主动搜索社区最佳实践；同一问题尝试 2 次以上仍未解决，必须停下来上网搜索官方文档、高星开源仓库、成熟技术博客，以被验证过的代码为参考，禁止继续瞎蒙
8. **如非必要不新建文件** — 优先在现有文件内扩展，确实需要模块隔离时再创建新文件
9. **运行前必检** — 每次运行代码前必须检查：编译错误（`tsc` / `vite`）、运行时错误、代码逻辑错误。不能带着明显错误提交
10. **文件大小控制** — 单文件不超过 500 行，超过必须合理拆分。禁止一个文件里塞所有逻辑
11. **目录扁平** — 目录层级不要超过 4 层，禁止无意义的路径嵌套
12. **文件名唯一** — 不同路径下禁止出现同名文件（`index.ts` 等规范命名除外），防止 import 混淆
13. **注释质量** — 必要处注释必须详细（复杂算法、边界情况、hack 原因），但禁止无意义的冗长注释（如 `// 定义变量 a`）

---

## Git Commit 规范

### 语言

**Commit message 必须使用中文**。禁止纯英文 commit message。

### 提交频率

- **不要每个文件修改都提交**。按功能批次提交，一个完整任务结束后统一 commit
- 例外：涉及大量文件（如重构、文档同步）时可以一次提交
- 反面教材：连续 5 个 commit 每个只改 1-2 行

### 新增文件处理

- **每次新增文件都必须显式 `git add`**，禁止依赖 `git commit -a`（`-a` 不会包含未跟踪文件）
- 提交前运行 `git status` 确认新增文件已被纳入暂存区
- 反面教材：代码里引用了新文件，但 commit 里漏了，导致别人拉下来编译失败

### Message 格式

```
<type>(<scope>): <简短描述>

<详细说明（可选）>
```

示例：
```
feat(github): 新增 PR 审查工具链

- 添加 github_create_pull_request_review
- 支持文件级评论和行级评论
- 修复 github_get_pull 返回字段缺失问题
```

常用 type：
| 类型 | 说明 |
|------|------|
| feat | 新功能 |
| fix | 修复 bug |
| docs | 文档更新 |
| refactor | 重构（无功能变化） |
| chore | 杂项（清理、配置等） |
| test | 测试相关 |

---

## 依赖管理规范

### 安装约束

- **每次安装新库后，必须立即更新项目依赖文件**：
  - Node.js：`package.json`
  - Python：`requirements.txt`
- **必须使用精确版本号**（`==` 或固定版本），禁止用 `^`、`>=`、`~` 等模糊范围
- 反面教材：本地装了一个库调试完就忘了写进 package.json，别人拉下来跑不通

### 版本锁定检查

- **定期扫描本地环境**，确认实际安装的版本与 `package.json` / `requirements.txt` 一致
- 发现版本漂移时，立即对齐（更新依赖文件或重装锁定版本）
- 工具参考：`scripts/check-versions.cjs`（Node 版本扫描）

### 临时文件管理

- **所有临时/实验性文件统一放入 `tmp/` 目录**，禁止散落在根目录或业务目录中
- 临时脚本、测试图片、调试日志等，用完即删或归档到 `tmp/`
- `.gitignore` 已配置 `tmp/*`（保留 `tmp/.gitkeep`），确保临时文件不会误提交

---

## 技术栈速查

| 层级 | 技术 |
|------|------|
| 前端 | Vue 3 + VitePress 1.x + Tailwind CSS 3.x |
| 状态 | Pinia + Vue Composition API |
| 后端 | Express 5.x（VitePress 插件集成） |
| AI | DeepSeek / Kimi / Zhipu / OpenAI / Gemini / Anthropic / Qwen / ... |
| 包管理 | pnpm |

---

## 关键目录

```
src/theme/tools/          # 工具系统（119+ 工具）
server/routes/            # BFF 路由
server/services/          # 后端服务
docs/guide/               # 用户文档
project/docs/             # 开发文档
project/requirements/     # 需求管理（backlog + completed）
.dev-log/                 # 开发日志（每次交互必须更新）
```

---

## 紧急联系

如果用户对日志记录不满意，立即检查最近一次 `.dev-log/session-*.md` 是否遗漏了思考过程或失败尝试。
