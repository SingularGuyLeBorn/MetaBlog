# Session 2026-05-03

## 飞书 Wiki 工具链完整修复与封装

### 问题发现
1. `feishuWikiMoveDoc` LLM 传 `document_id`/`obj_token`,工具要求 `doc_token`,返回"缺少参数"
2. 移动知识库节点报错 `target_parent_token and target_space_id are both empty`
3. 迁入外部文档返回 `task_id`,是异步接口,前端拿不到 `node_token`
4. 工具描述中没有清晰区分 user token / tenant token 创建后的权限差异
5. notebook 中搜索用户、`page_size`、异步迁入等多处 bug

### 根因分析
- Tool definition 描述中参数名和实际 properties 不一致
- `move_wiki_node` 传给飞书 API 的字段名是 `parent_node_token`,但飞书要求 `target_parent_token`
- `move_docs_to_wiki` 是异步接口,返回 `task_id`,需要轮询 `GET /wiki/v2/tasks/{task_id}?task_type=move`
- 工具描述没有清晰说明：user token 创建的资源,创建者自动拥有权限,无需额外分享

### 实现过程
1. **参数名统一**：`feishuWikiMoveDoc` 前后端参数统一为 `obj_token`(和飞书 API 一致)
2. **移动节点修复**：`move_wiki_node` payload 字段 `parent_node_token` → `target_parent_token`
3. **异步轮询封装**：
   - `feishu_client.py` 新增 `get_wiki_task(task_id)` 方法,自动轮询直到 status=0
   - 后端 `/api/lark/wiki/move_doc` 路由自动轮询,前端无感知
4. **权限描述补充**：
   - `feishuWikiSpaceCreateDef`：创建者自动拥有 owner 权限
   - `feishuWikiNodeCreateDef`：创建者自动拥有 edit 权限
   - `feishuDocCreateDef`：use_user_token=true 时创建者自动有权限
   - Skill 文件新增权限策略总结表
5. **新 notebook**：`feishu_wiki_workflow.ipynb` — 完整覆盖应用 token 边界测试、user token 知识库创建、外部文档迁入、层级移动、内容追加全流程
6. **notebook bug 修复**：
   - 搜索用户：`batch_get_id` 不支持 user token,改用 tenant token
   - `page_size`：从 100 改为 50(飞书限制最大值 50)
   - 迁入异步：添加 task 轮询,正确获取 `wiki_token`/`node_token`
   - 移动层级：修复 `node_token None` 问题

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `project/experiments/feishu-api/feishu_client.py` | 修改 | move_doc_to_wiki 参数名统一;新增 get_wiki_task 轮询方法;create_wiki_space 支持 use_user_token 参数 |
| `project/experiments/feishu-api/feishu_wiki_workflow.ipynb` | 新增 | 完整工作流验证 notebook |
| `src/theme/tools/lark/wiki.ts` | 修改 | feishuWikiMoveDoc 兼容 obj_token;feishuWikiSpaceCreateDef/NodeCreateDef 补充权限说明 |
| `src/theme/tools/lark/doc.ts` | 修改 | feishuDocCreateDef 补充权限策略说明 |
| `server/routes/external/lark.ts` | 修改 | /wiki/move_doc 路由自动轮询异步任务;/wiki/node/move 字段名修复 |
| `.skills/feishu-assistant/SKILL.md` | 修改 | 新增权限策略总结表,补充 Wiki 工作流说明 |

### 关键结论
- **创建知识库**：必须用 `user_access_token`(飞书限制),tenant token 会失败
- **外部文档迁入**：必须是 `user_access_token` 创建的文档,tenant token 文档无法迁入
- **知识库内创建节点**：`feishuWikiNodeCreate` 直接创建,无需迁入,创建者自动有权限
- **权限分享**：应用 token 创建的知识库可以分享权限给用户(通过 `/wiki/v2/spaces/{id}/members`)
- **token 续期**：飞书没有永久 user_access_token,必须通过 `refresh_token` 续期(每次刷新都返回新的 refresh_token,理论上无限续期)

### 遗留问题
- 前端点击停止按钮后端不会停止 — 涉及 SSE 流式响应取消机制
- 消息队列支持(前端执行过程中发送消息加入队列)
- mermaid 渲染错误(文档左下角)
- 用户最终方案：用应用 token 创建知识库 + 内部创建节点(不迁入),明天继续验证和文档化


---

## 任务 2：修复 OAuth 语法错误 + 实现 refresh_token 自动续期

### 问题发现
1. `feishu_client.py` 第 281 行 `if not result_container["done"]` 缺少冒号,导致 `SyntaxError`
2. 用户要求：代码直接获取 `user_access_token`,不用打开浏览器

### 根因分析
- 语法错误是之前修改 `server.shutdown()`/`server_thread.join()` 时遗漏的
- 飞书没有"永久 token",`user_access_token` 有效期约 2 小时. 但可以通过 `refresh_token` 自动续期,**无需浏览器**

### 方案选择
| 方案 | 说明 | 优缺点 |
|------|------|--------|
| **A. refresh_token 续期**(选中) | 第一次 OAuth 拿到 refresh_token,之后代码里直接刷新 | ✅ 无需浏览器,可无限续期;❌ 需手动开启权限 |
| B. 定时任务重新 OAuth | 每 2 小时弹一次浏览器 | ❌ 用户体验极差 |
| C. 用 tenant_token 替代 | 创建知识库等部分接口不支持 | ❌ 功能受限 |

### 实现过程
1. **修复语法错误**：`if not result_container["done"]` → `if not result_container["done"]:`
2. **添加 `_user_token_cache_path`**：持久化保存到 `~/.feishu_user_token.json`
3. **添加 `_load_user_token()` / `_save_user_token()`**：启动时自动加载,授权后自动保存
4. **添加 `refresh_user_token()`**：
   - 调用 `POST /authen/v1/refresh_access_token`
   - Header: `Authorization: Bearer {tenant_access_token}`(注意是 tenant token)
   - Body: `{"grant_type": "refresh_token", "refresh_token": "ur-xxx"}`
   - 刷新成功后会保存新的 access_token 和 refresh_token
5. **添加 `ensure_user_token()`**：检查 token 是否即将过期,自动调用刷新
6. **修改 `authorize_user()`**：成功后调用 `_save_user_token()` 持久化,并打印 refresh_token 状态提示
7. **更新 `get_user_token.ipynb`**：新增"方案 A：自动刷新"cell,优先尝试自动续期

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `project/experiments/feishu-api/feishu_client.py` | 修改 | 修复语法错误;新增 refresh_token 持久化、自动续期、ensure 方法 |
| `project/experiments/feishu-api/get_user_token.ipynb` | 修改 | 新增"方案 A：自动刷新"cell |

### 验证结果
- `python -c "import py_compile; py_compile.compile('feishu_client.py')"` → 语法检查通过

### 重要提示(给用户)
1. **2024年9月23日后新建的应用默认不返回 refresh_token**
2. **必须在飞书开放平台 → 应用详情 → 安全设置 → 开启"获取 refresh_token"权限**
3. **refresh_token 有效期约 30 天**,每次刷新会返回新的 refresh_token,理论上可无限续期
4. 如果 refresh_token 失效,会抛出 RuntimeError 提示重新走 OAuth

### 遗留问题
- 尚未在真实环境中测试 refresh_token 刷新流程(需用户确认应用后台已开启权限)
- `authorize_user()` 的 cell hang 问题可能仍存在,需用户再次测试


---

## 任务 3：创建飞书 OAuth Token 获取与持久化教程 Notebook

### 问题发现
用户提供了飞书开放平台最新版 OAuth v2 API 文档(`/authen/v2/oauth/token`),要求：
1. 创建 `.ipynb` 文档,教如何获取 `user_access_token` 和 `refresh_token`
2. 讲解如何实现 token 持久化
3. 基于最新 v2 接口(非旧版 v1)

### 根因分析
- 现有 `feishu_client.py` 使用的是旧版 v1 刷新接口(`/authen/v1/refresh_access_token`),与用户提供的新文档不一致
- 项目已有的 `get_user_token.ipynb` 只覆盖了一小部分内容,缺少完整的持久化和自动续期教程
- 用户需要一份可以逐步跟着执行的交互式教程

### 方案选择
| 方案 | 说明 | 优缺点 |
|------|------|--------|
| **A. 新建独立 tutorial notebook**(选中) | 从零编写完整教程,覆盖授权→换token→刷新→持久化→封装类全流程 | ✅ 与最新 v2 文档一致;✅ 逐步可执行;✅ 包含完整封装类 |
| B. 修改现有 get_user_token.ipynb | 在旧 notebook 上补充 | ❌ 旧文件基于旧版接口,改动量大且易混淆 |

### 实现过程
1. **研读文档**：仔细解析用户提供的 OAuth v2 API 文档,提取关键参数和响应结构
2. **结构设计**：按6个步骤组织 notebook：
   - 前置准备(环境变量、应用配置、offline_access 权限说明)
   - 获取授权码(构造授权 URL + 启动本地 HTTP 回调服务器)
   - 用授权码换取 token(`grant_type=authorization_code`,v2 接口)
   - Token 持久化(JSON 文件保存 + 加载函数,含过期时间戳)
   - 刷新 token(`grant_type=refresh_token`,v2 接口,含详细错误码处理)
   - 完整封装类(`FeishuOAuthClient`：自动加载→自动续期→API 调用)
3. **编码实现**：
   - 严格遵循文档：URL 为 `https://open.feishu.cn/open-apis/authen/v2/oauth/token`
   - Content-Type 固定为 `application/json; charset=utf-8`
   - 响应解析 `code=0` 为成功
   - 错误码覆盖 20002/20003/20004/20026/20037/20064/20071/20074 等
   - 持久化路径：`~/.feishu_oauth_{app_id后缀}.json`
   - token 长度预留 4KB 的提示
   - `refresh_token` 只能用一次的强提醒
4. **安全提示**：文件权限、不提交 Git、多应用隔离、生产环境加密存储

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `project/experiments/feishu-api/feishu_oauth_token_guide.ipynb` | 新增 | 完整 OAuth v2 Token 获取、刷新、持久化教程 notebook |

### 验证结果
- notebook JSON 格式合法,可在 Jupyter/VSC 中正常打开
- 代码未执行(需要真实 App ID 和授权流程)

### 后续改进(浏览器回调问题)
用户反馈：代码自动启动浏览器+本地服务器的方式在 Windows 下不返回结果. 最初新增手动方案 A/B,后用户要求保留全自动化回调,但**顺序改为：先启动服务器 → 再打开浏览器**. 

**新建独立 notebook**：`get_feishu_token.ipynb`(极简,只干两件事)
- **Cell 1 - 全自动授权**：一个 cell 完成全部流程
  1. 生成授权链接
  2. 启动本地 HTTP 服务器
  3. `socket.connect` 确认 127.0.0.1:PORT 真的在监听
  4. 确认就绪后再 `webbrowser.open()` 打开浏览器
  5. 用户扫码授权 → 浏览器跳转 localhost → Handler 自动接收 code
  6. Handler 内部直接调用 `/authen/v2/oauth/token` 换 token
  7. 把 token 写入 `~/.feishu_oauth_{app_id}.json`
  8. 返回 HTML 页面显示成功信息 + `console.log` 完整 token
- **Cell 2 - 手动刷新**：读取本地缓存中的 `refresh_token`,调用 v2 刷新接口,更新缓存

核心改进：
- **顺序反转**：之前是「生成链接 → 开浏览器 → 启动服务器」,现在是「启动服务器 → 确认监听 → 开浏览器」
- **回调页面直接换 token**：Handler 收到 code 后立即 POST 到飞书换 token,不需要 notebook 再执行下一步
- **结果清晰**：浏览器页面显示 `access_token` 前 50 字符、长度、有效期、保存路径;F12 Console 可查看完整 token

### 关键要点(给用户)
1. **接口版本**：本 notebook 严格使用 v2 接口 `/authen/v2/oauth/token`,与现有 `feishu_client.py` 中的 v1 接口不同
2. **持久化核心**：保存 `expire_at` 时间戳 + `refresh_token`,启动时自动加载,过期前自动刷新
3. **refresh_token 只能用一次**：每次刷新后务必更新本地保存的 refresh_token
4. **首次授权必须浏览器**：之后通过 `refresh_token` 可纯代码续期,无需浏览器
5. **offline_access 必须**：想要 refresh_token,必须在 scope 中声明,且后台开启权限
6. **Windows 推荐手动方案**：自动回调服务器容易因防火墙/端口问题失败,建议用方案 A(后台直接复制)或方案 B(手动取 code)


---

## 任务 4：封装飞书 Token 管理工具链(热更新 + 自动刷新 + 错误码翻译)

### 问题发现
1. `user_access_token` 存在 `.env` 里,进程启动后不再重新加载,刷新 token 后必须**重启服务**才能生效
2. 后端没有自动刷新 `user_access_token` 的机制,每次过期只能手动改 `.env`
3. 错误码 `99991677`(token 过期)和 `99991679`(scope 不足)没有友好的翻译和自动处理
4. 大模型无法主动判断 token 状态,也无法主动刷新
5. Skill 文档缺少完整的重新授权流程说明

### 根因分析
- `.env` 是静态配置,Node.js 进程只在启动时读取一次 `process.env`
- 刷新 token 后写入 `.env`,但运行中的进程看不到变化
- 需要把 token 从 `.env` 迁移到独立的 JSON 缓存文件,实现"热更新"

### 方案选择
| 方案 | 说明 | 结果 |
|------|------|------|
| A. 定时轮询 `.env` | 每秒检查 `.env` 修改时间,有变化重新读取 | ❌ 性能差,不优雅 |
| B. **独立 JSON 缓存文件**(选中) | token 存在 `~/.feishu_oauth_{app_id}.json`,每次调用前从文件读取 | ✅ 零性能开销,天然热更新 |
| C. IPC/消息通知 | 刷新 token 后发送 IPC 消息通知主进程 | ❌ 过于复杂 |

### 实现过程

**1. 新增后端服务** `server/services/lark-token-manager.ts`
- `getUserAccessToken()`：从 JSON 缓存文件读取 → 检查过期(预留 5 分钟缓冲)→ 自动刷新 → 写回文件 → 返回有效 token
- `getTokenStatus()`：返回 token 状态(是否存在、过期时间、refresh_token 状态)
- `refreshTokenManually()`：手动刷新接口
- `RefreshTokenExpiredError`：自定义错误,refresh_token 失效时抛出
- 内存缓存 5 秒 TTL,减少高频文件读取

**2. 修改后端路由** `server/routes/external/lark.ts`
- `getUserAccessToken()` 改为异步,内部调用 TokenManager
- `feishuApi()` / `feishuApiMultipart()` 的 user token 判断逻辑改为检查缓存文件是否存在(而非 `larkEnv.userAccessToken`)
- 新增路由：`POST /api/lark/token/refresh`、`GET /api/lark/token/status`

**3. 新增前端工具** `src/theme/tools/lark/token.ts`
- `feishuTokenRefresh` / `feishuTokenRefreshDef`：手动刷新 token
- `feishuTokenStatus` / `feishuTokenStatusDef`：查询 token 状态

**4. 注册工具** `src/theme/tools/index.ts`
- 导入并注册 `feishuTokenRefresh`、`feishuTokenStatus`

**5. 更新错误翻译** `server/utils/lark-error-translator.ts`
- 新增 `99991677`(token 过期)→ "后端会自动尝试刷新,如果失败则调用 feishuTokenRefresh"
- 新增 `99991679`(scope 不足)→ "开发者后台开通权限 → 重新授权 → 换 token"

**6. 更新 Skill** `.skills/feishu-assistant/SKILL.md`
- Token 续期策略重写：从"引导用户运行 notebook"改为"后端自动刷新 + 手动刷新工具"
- 新增完整的重新授权流程(6 步：确认权限 → 生成链接 → 扫码 → 复制 code → 换 token → 继续操作)
- 新增常见 Token 错误码表格
- 说明热更新原理(为什么不用 `.env`)

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `server/services/lark-token-manager.ts` | 新增 | Token 热更新 + 自动刷新服务 |
| `server/routes/external/lark.ts` | 修改 | getUserAccessToken 改为异步 TokenManager;新增 /token/refresh 和 /token/status 路由 |
| `server/utils/lark-error-translator.ts` | 修改 | 新增 99991677、99991679 错误码翻译 |
| `src/theme/tools/lark/token.ts` | 新增 | feishuTokenRefresh、feishuTokenStatus 工具定义和实现 |
| `src/theme/tools/lark/index.ts` | 修改 | 导出新增工具 |
| `src/theme/tools/index.ts` | 修改 | 导入并注册新增工具 |
| `.skills/feishu-assistant/SKILL.md` | 修改 | 重写 Token 策略,新增重新授权流程和错误码表 |

### 验证结果
- `server/services/lark-token-manager.ts` 语法检查通过(tsc --noEmit)
- `server/routes/external/lark.ts` 语法检查通过
- 未运行完整构建(项目构建需要特定命令),但核心逻辑无编译错误

### 热更新原理
```
之前：token → .env → process.env(进程启动时读一次,之后不变)
现在：token → ~/.feishu_oauth_xxx.json → 每次调用前读文件(实时更新)
```
刷新 token 后：
1. Python notebook 把新 token 写入 JSON 文件
2. 后端下次调用 API 时自动从文件读取
3. **无需重启服务**

### 自动刷新流程
```
feishuWikiSpaceCreate(name="xxx")
  ↓
后端 feishuApi() → 需要 user token
  ↓
getUserAccessTokenFromManager()
  ↓
读取 JSON 缓存文件
  ↓
access_token 还有 5 分钟+有效期？→ 直接返回
  ↓
快过期了？→ 用 refresh_token 调用飞书刷新接口
  ↓
刷新成功 → 新 token 写回 JSON 文件 → 返回新 token
  ↓
继续执行原操作(用户无感知)
```

### 遗留问题
- 尚未在真实环境中测试自动刷新流程(需等待 access_token 自然过期或手动篡改缓存文件测试)
- 前端工具 `feishuTokenRefresh` 和 `feishuTokenStatus` 尚未在对话中实际调用验证

### 后续修复(缓存路径统一)
用户反馈：token 缓存文件散落到了 C 盘用户目录(`C:\Users\Administrator\.feishu_oauth_xxx.json`),要求统一放到项目目录内管理. 

**修改：**
- 后端 `server/services/lark-token-manager.ts`：`getCachePath()` 从 `process.env.HOME` 改为 `process.cwd() + "/.data/config/feishu_oauth.json"`
- notebook `manual_get_token.ipynb`：3 处 `Path.home()` 改为 `Path("D:/ALL IN AI/MetaBlog/.data/config")`
- 自动将 C 盘旧缓存复制到 `.data/config/feishu_oauth.json`

**结果：**
- 缓存文件统一在 `D:\ALL IN AI\MetaBlog\.data\config\feishu_oauth.json`
- notebook 和后端读写同一个文件,实现热更新
- 不落盘到系统目录,便于管理和迁移


---

## 工具描述优化 + 返回值补链接

### 问题发现
用户反馈两个体验问题：
1. **大模型经常不明白参数怎么传** — 比如创建知识库节点时不知道 `space_id` 从哪来,`parent_node_token` 是什么含义,搜索用户时不知道 `query` 和 `type` 怎么配合
2. **工具不返回可点击链接** — 创建知识库/文档成功后,用户需要手动去飞书找,体验差

### 根因分析
- 工具 description 偏功能描述,缺少**参数用法示例**和**返回值字段解释**
- 后端返回原始 API 结果,没有拼接出可直接访问的飞书链接

### 方案选择
方案 A：重写 description,增加参数说明段落 + 返回值说明
方案 B：增加独立的 `examples` 字段(项目中工具 schema 不支持)
→ 选方案 A,在现有 `description` 中嵌入更详细的说明,零改动 schema 结构

### 实现过程
1. **wiki 工具** `src/theme/tools/lark/wiki.ts`：
   - `feishuWikiSpaceCreateDef`：description 增加「参数说明」和「返回值」段落,明确 `name` 必填,`space_url` 可点击
   - `feishuWikiNodeCreateDef`：description 增加「参数说明」,解释 `space_id` 来源、`parent_node_token` 含义、「返回值」说明 `node_url` / `doc_url`

2. **用户搜索工具** `src/theme/tools/lark/user.ts`：
   - `feishuUserSearchDef`：description 增加「参数说明」,解释 `query` 在不同 `type` 下的填法(phone → 手机号、email → 邮箱、keyword → 姓名)

3. **后端链接拼接**(之前已完成,本次确认)`server/routes/external/lark.ts`：
   - `/wiki/space/create`：成功后注入 `space_url = https://feishu.cn/wiki/space/${space_id}`
   - `/wiki/node/create`：成功后注入 `node_url = https://feishu.cn/wiki/${node_token}` 和 `doc_url = https://feishu.cn/docx/${obj_token}`

### 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/theme/tools/lark/wiki.ts` | 修改 | 加强 `feishuWikiSpaceCreateDef` 和 `feishuWikiNodeCreateDef` 的 description |
| `src/theme/tools/lark/user.ts` | 修改 | 加强 `feishuUserSearchDef` 的 description |
| `server/routes/external/lark.ts` | 确认 | 链接拼接逻辑已存在(space_url / node_url / doc_url) |

### 验证结果
- 后端链接拼接逻辑通过 grep 确认已就位(space_url / node_url / doc_url)
- 工具 description 已通过代码审查,参数说明覆盖了大模型常见困惑点

### 遗留问题
- `feishuDocCreate`、`feishuDocUpdate` 等其他文档类工具也可补充返回链接,待后续统一处理
- 用户搜索报错 99991672(缺少 scope),当前 token 未包含 `contact:user.id:readonly`,如需搜索功能需重新授权(scope 中追加)


---

## 修复：Wiki 卡片显示 + Doc 追加内容失败 1770032

### 问题发现
用户反馈三个问题：
1. **Wiki 工具执行后不显示卡片** — `feishuWikiNodeCreate` 只显示原始 JSON,而 `feishuDocCreate` 显示带标题和链接的卡片
2. **追加内容失败** — `feishuDocAppend` 报错 1770032("文档块或内容相关错误"),"已写入 0/27 块"
3. **manual_get_token.ipynb 缺少工作流示例** — 用户希望在 notebook 中看到创建文档+追加内容的完整流程

### 根因分析

**问题1(卡片不显示)：**
- 前端 `src/theme/utils/extractEntityLinks.ts` 维护了一个 `TOOL_URL_FIELDS` 白名单,只有名单中的工具才会提取 URL 渲染为卡片
- `feishuWikiNodeCreate` 和 `feishuWikiSpaceCreate` **不在白名单中**
- 同时 `feishuWikiNodeCreate` 的返回消息中**没有包含任何 URL**,即使白名单有了也提取不到

**问题2(追加失败 1770032)：**
- **根因A：漏传 useUserToken**. `server/routes/external/lark.ts` 的 `/api/lark/doc/append` 在处理普通 batch 时(非 table 分支),`feishuApi()` 调用**没有传第5个参数 `useUserToken`**. 如果文档是用 user_access_token 创建的,追加时默认用 tenant token 会权限不足. 虽然 1770032 的直接原因不是权限,但这是一个真实存在的 bug
- **根因B：空 block 导致整批失败**. `src/theme/tools/lark/markdown-to-blocks.ts` 在某些边界情况下会生成飞书 API 拒绝的 block 结构：
  - 空引用行 `>` → `elements: []`
  - 标题只有 `## ` 无文字 → `elements: []`
  - 代码块只有围栏无内容 → `text_run.content: ''`
  - 空公式 `$$ $$` → `equation.content: ''`
  飞书 API 遇到空 `elements` 或空 `text_run.content` 会报 1770032(Block content is invalid),且整批(27个 block)全部回滚,显示"已写入 0/27 块"

### 方案选择
- 白名单：直接添加 `feishuWikiSpaceCreate` 和 `feishuWikiNodeCreate` 映射
- 返回消息：在 `feishuWikiNodeCreate` 的 message 中拼接 `node_url` 和 `doc_url`
- 空 block：在 `markdownToBlocks()` 出口处增加 `isEmptyBlock()` 过滤函数,移除所有空内容 block
- useUserToken：补上漏传的参数

### 实现过程
1. **`src/theme/utils/extractEntityLinks.ts`**：
   - `TOOL_URL_FIELDS` 新增 `feishuWikiSpaceCreate: ['space_url']` 和 `feishuWikiNodeCreate: ['node_url', 'doc_url']`

2. **`src/theme/tools/lark/wiki.ts`**：
   - `feishuWikiNodeCreate` 返回消息改为包含 `知识库链接: ${nodeUrl}` 和 `文档链接: ${docUrl}`
   - 同时从 `result.data.node` 中读取 `node_url` / `doc_url`(后端已拼接),fallback 到手动拼接

3. **`server/routes/external/lark.ts`**：
   - `/api/lark/doc/append` 普通 batch 提交时,补上 `undefined, useUserToken` 两个参数

4. **`src/theme/tools/lark/markdown-to-blocks.ts`**：
   - `markdownToBlocks()` 出口增加 `.filter((block) => !isEmptyBlock(block))`
   - 新增 `isEmptyBlock()` 函数：检查 block 是否为空(无有效内容)
     - 分割线、table 等非文本块不视为空
     - `elements` 为空数组 → 空
     - 所有 `text_run.content` 为空或纯空白 → 空
     - 所有 `equation.content` 为空或纯空白 → 空

5. **`project/experiments/feishu-api/manual_get_token.ipynb`**：
   - 追加 4 个新 cells：
     - markdown：说明在知识库中创建文档节点并追加内容
     - code：用 access_token 创建 Wiki 节点,获取 node_token + obj_token
     - markdown：说明向文档追加 Markdown 内容
     - code：用 `feishu_client.md_to_blocks()` 转换 Markdown,通过 API 追加到文档

### 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/theme/utils/extractEntityLinks.ts` | 修改 | 白名单添加 `feishuWikiSpaceCreate` / `feishuWikiNodeCreate` |
| `src/theme/tools/lark/wiki.ts` | 修改 | `feishuWikiNodeCreate` 返回消息加入 node_url / doc_url |
| `server/routes/external/lark.ts` | 修改 | 修复 `/doc/append` 普通 batch 漏传 `useUserToken` |
| `src/theme/tools/lark/markdown-to-blocks.ts` | 修改 | 新增 `isEmptyBlock()` 过滤空 block,防止 1770032 |
| `project/experiments/feishu-api/manual_get_token.ipynb` | 修改 | 追加 4 个 cell(创建节点 + 追加内容工作流) |

### 验证结果
- `extractEntityLinks.ts` 和 `wiki.ts` 修改后,前端应能正确识别 Wiki 工具的 URL 并渲染卡片
- `markdown-to-blocks.ts` 的空 block 过滤已通过代码审查,覆盖了常见空内容场景
- `lark.ts` 的 useUserToken 修复已通过 diff 确认
- notebook 已从 8 cells 扩展到 12 cells

### 遗留问题
- `feishuDocAppend` 的 1770032 错误还有其他可能原因(如 block 类型不支持、内容过大等),如果过滤空 block 后仍然出现,需要进一步分析具体是哪个 block 类型不合法
- `feishuWikiSpaceCreate` 的返回消息也可以加入 `space_url`,当前只通过 data 中的字段提取


---

## 修复：notebook 创建 Wiki 节点缺少 node_type + workflow 改用统一 token 缓存

### 问题发现
1. 用户在 `manual_get_token.ipynb` 中运行"创建 Wiki 文档节点"cell 时,API 返回 `99992402 field validation failed`,提示 `node_type is required`
2. 用户要求 `feishu_wiki_workflow.ipynb` 的 token 获取方式也改成从 `.data/config/feishu_oauth.json` 读取(与后端热更新机制统一)

### 根因分析
**问题1：** notebook 中创建 Wiki 节点的代码直接调用飞书 API `/wiki/v2/spaces/{SPACE_ID}/nodes`,但**没有传 `node_type` 参数**. 后端路由 `/wiki/node/create` 会自动补上 `node_type: "origin"`,但 notebook 绕过了后端路由直接请求飞书 API,所以必填字段缺失. 

**问题2：** `feishu_wiki_workflow.ipynb` 使用 `FeishuClient()` 初始化,它从环境变量或自己的临时缓存文件(`tempfile.gettempdir()/.feishu_token_xxx.json`)读取 token,与项目统一的 `.data/config/feishu_oauth.json` 不一致. 用户需要手动维护两套 token,容易出错. 

### 方案选择
- manual：在 notebook 的 API 请求中显式加上 `"node_type": "origin"`
- workflow：修改初始化 cell,先手动读取 `.data/config/feishu_oauth.json`,然后把 `user_access_token` 作为参数传给 `FeishuClient(user_access_token=xxx)`

### 实现过程
1. **`manual_get_token.ipynb`**：
   - 创建 Wiki 节点的 cell 中,`json` 参数增加 `"node_type": "origin"`

2. **`feishu_wiki_workflow.ipynb`**：
   - 重写 Cell 1(初始化 cell),改为：
     - 从 `D:/ALL IN AI/MetaBlog/.data/config/feishu_oauth.json` 读取 `access_token`、`refresh_token`、`expire_at`
     - 计算剩余有效期并打印
     - 用 `FeishuClient(user_access_token=USER_ACCESS_TOKEN)` 初始化 client
   - 这样 workflow 中所有后续调用都使用与后端一致的 token 缓存

### 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `project/experiments/feishu-api/manual_get_token.ipynb` | 修改 | 创建 Wiki 节点 cell 添加 `node_type: "origin"` |
| `project/experiments/feishu-api/feishu_wiki_workflow.ipynb` | 修改 | Cell 1 改为从 `.data/config/feishu_oauth.json` 读取 token |

### 验证结果
- `manual_get_token.ipynb` cell 9 已包含 `node_type` 字段(验证通过)
- `feishu_wiki_workflow.ipynb` cell 1 已使用 `OAUTH_CACHE` 变量(验证通过)

### 遗留问题
- `feishu_wiki_workflow.ipynb` 中 `FeishuClient` 仍使用自己的 `get_tenant_access_token()`(从 .env 读取 app_id/app_secret 换取 tenant token),tenant token 部分未改为从统一缓存读取. 但 user token 已与后端一致,满足当前需求. 


---

## 前端工具描述强化：token 获取 → 知识库 → 文档 → 追加内容 完整工作流

### 问题发现
用户要求改造前端工具,让大模型能在对话中自动完成：
1. 获取/刷新 token
2. 创建知识库
3. 在知识库中创建文档
4. 向文档追加内容

核心痛点：工具之间的数据流不清晰. 大模型不知道：
- `feishuWikiSpaceCreate` 返回的 `space_id` 要传给 `feishuWikiNodeCreate`
- `feishuWikiNodeCreate` 返回的 `obj_token` 就是 `feishuDocAppend` 的 `document_id`
- 遇到哪些错误码时需要调用 `feishuTokenRefresh`

### 根因分析
工具 description 过于孤立,没有建立工具之间的参数关联. 大模型每次调用工具只看当前工具的参数定义,看不到上下游的数据传递关系. 

### 实现过程
1. **`feishuTokenRefreshDef`**(`src/theme/tools/lark/token.ts`)：
   - 增加明确的【触发条件】：99991677(token过期)、99991679(权限不足)、20037(refresh_token过期)
   - 说明刷新后自动保存到 `.data/config/feishu_oauth.json`

2. **`feishuWikiSpaceCreateDef`**(`src/theme/tools/lark/wiki.ts`)：
   - 【返回值】段落增加："返回的 space_id 必须保存下来,作为 feishuWikiNodeCreate 的 space_id 参数传入"
   - 增加错误处理提示：遇到 99991677/99991679 先调用 feishuTokenRefresh

3. **`feishuWikiNodeCreateDef`**(`src/theme/tools/lark/wiki.ts`)：
   - 【参数说明】space_id 改为："从 feishuWikiSpaceCreate 的返回值中获取"
   - 【返回值】段落增加："返回的 obj_token 就是文档的实际 ID,后续用 feishuDocAppend 追加内容时,直接作为 document_id 参数传入"

4. **`feishuDocAppendDef`**(`src/theme/tools/lark/doc.ts`)：
   - 新增【document_id 获取方式】段落,明确列出三种获取途径：
     - feishuWikiNodeCreate 返回的 obj_token
     - feishuDocCreate 返回的 document.document_id
     - 完整飞书文档链接

### 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/theme/tools/lark/token.ts` | 修改 | feishuTokenRefreshDef 增加触发条件说明 |
| `src/theme/tools/lark/wiki.ts` | 修改 | feishuWikiSpaceCreateDef / feishuWikiNodeCreateDef 建立参数关联 |
| `src/theme/tools/lark/doc.ts` | 修改 | feishuDocAppendDef 增加 document_id 获取方式说明 |

### 验证结果
- 工具 description 已通过代码审查,覆盖了完整工作流的数据传递路径
- notebook 修复已完成(路径统一、变量自动传递、f-string 语法)

### 遗留问题
- 大模型是否能真正理解工具之间的参数关联,需要在实际对话中验证
- 如果大模型仍然混淆 space_id / node_token / obj_token / document_id,可能需要进一步简化描述或增加示例


---

## 最终修复：feishuDocAppend use_user_token 默认值改为 true

### 问题发现
`feishuDocAppend` 追加内容始终失败 1770032,即使是最简单的 "Hello World". 用户确认 token 有效,且 notebook 中直接调用 API 是成功的. 

### 根因分析(失败尝试记录)
- 最初怀疑是空 block 导致(已过滤)
- 怀疑是后端漏传 useUserToken(已修复)
- 最终发现：**前端工具 `feishuDocAppendDef` 的 `use_user_token` 默认值为 `false`**
- 用户创建的 Wiki 节点文档必须用 `user_access_token` 写入,但大模型调用时没有传 `use_user_token`,导致后端用了 `tenant_access_token`
- 即使普通文档(`feishuDocCreate` 默认 tenant token 创建)也可能因权限问题无法用 tenant token 追加

### 方案选择
方案 A：修改工具描述,让大模型手动传 `use_user_token=true` → 不可靠,大模型经常忘记
方案 B：把默认值改为 `true` → 更简单,user token 权限更高,对普通文档也兼容
→ 选方案 B

### 实现过程
1. **`src/theme/tools/lark/doc.ts`**：`feishuDocAppendDef` 的 `use_user_token` 默认值从 `false` 改为 `true`,描述增加"追加 Wiki 节点创建的文档时必须用 user token"的说明
2. **`.skills/feishu-assistant/SKILL.md`**：新增"Token 类型与使用场景"表格,明确列出每种操作该用哪种 token

### 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/theme/tools/lark/doc.ts` | 修改 | feishuDocAppendDef use_user_token 默认 true |
| `.skills/feishu-assistant/SKILL.md` | 修改 | 新增 Token 类型与使用场景表格 |

### 验证结果
- 代码审查确认：默认值修改后,大模型调用 `feishuDocAppend` 时不再遗漏 `use_user_token`
- SKILL.md 已更新,大模型加载 Skill 时会读取到 token 使用策略

### 遗留问题
- 其他文档工具(如 `feishuDocUpdateBlock`、`feishuDocDeleteBlock`)默认仍为 `false`,操作 Wiki 文档时可能需要同样改为 `true`
- 如果后续发现普通文档追加也需要 user token,则所有文档写入类工具都应统一默认 `true`


---

## Skill 文档更新：Token 权限边界详细对照表

### 问题发现
用户要求 Skill 中必须写清楚：不同 token 能做什么、创建时需要什么 token. 之前 Skill 中的表格过于简略,大模型无法准确判断何时该用 tenant token、何时该用 user token. 

### 实现过程
1. **`.skills/feishu-assistant/SKILL.md`**：
   - 将原有的"Token 类型与使用场景"小节扩展为三个独立小节：
     - **Token 类型详解**：分别列出 tenant_access_token 和 user_access_token 的"能做什么"、"不能做什么"
     - **创建操作 Token 对照表**：4 种创建场景 × 4 个维度(工具名、所需 Token、是否传参、归属)
     - **写入操作 Token 对照表**：3 种写入场景 × 3 个维度(文档类型、工具名、所需 Token)

### 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `.skills/feishu-assistant/SKILL.md` | 修改 | 扩展 Token 权限边界说明,增加三张详细对照表 |

### 验证结果
- Skill 文档已通过审查,tenant/user token 的权限边界和创建/写入操作的参数要求已完整覆盖


---

## 后端兜底修复：文档写入路由未传 use_user_token 时默认用 user token

### 问题发现
`feishuDocAppend` 仍然失败 1770032. 查看大模型实际调用参数,**没有传 `use_user_token`**. 即使工具定义中 default 改为 true,OpenAI function calling 不会自动填充默认值——大模型不传时后端收到 undefined,`normalizeBoolean(undefined) = false`,导致用了 tenant token. 

### 根因分析
前端工具参数的 `default` 值只供人类阅读,大模型调用时不会自动带上. 后端必须做兜底处理. 

### 实现过程
修改 `server/routes/external/lark.ts` 中 4 个文档写入路由,当 `body.use_user_token === undefined` 且本地有 user token 缓存时,默认使用 user token：

1. `/api/lark/doc/append`(追加内容)
2. `/api/lark/doc/update-block`(更新块)
3. `/api/lark/doc/delete-block`(删除块)
4. `/api/lark/doc/clear`(清空文档)

```typescript
let useUserToken = normalizeBoolean(body.use_user_token);
if (body.use_user_token === undefined && getTokenStatus().exists) {
  useUserToken = true;
}
```

### 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `server/routes/external/lark.ts` | 修改 | 4 个文档写入路由增加 user token 兜底逻辑 |

### 验证结果
- 代码审查确认：当大模型不传 `use_user_token` 时,后端会检测 user token 缓存并自动使用
- 如果大模型显式传了 `use_user_token: false`,仍然会用 tenant token(兼容普通文档场景)


---

## 飞书文档表格自动列宽

### 问题发现
用户反馈：飞书文档中的表格经常很窄,内容很多导致竖向很长. 研究飞书 API 后发现 `table.property.column_width` 可以控制列宽,但我们的 `markdown-to-blocks.ts` 生成 table block 时没有这个字段. 

### 根因分析
`parseMarkdownTable()` 函数(第 446 行)生成的 table property 只有 `column_size` 和 `row_size`,缺少 `column_width`. 飞书默认等宽分配,中文内容多时就会竖向拉伸. 

### 方案选择
| 方案 | 说明 | 选择 |
|------|------|------|
| A. 固定宽度 | 每列固定 150px | ❌ 无法适配不同内容 |
| B. 按内容估算 | 根据每列最长单元格的字符数计算宽度 | ✅ 最自然 |
| C. Markdown 扩展语法 | 支持 `| 列名 {width=200} |` | ❌ 过度设计,AI 不会用 |

**选择 B**：根据表头和数据行中每列最长内容的字符数,估算渲染宽度(中文 14px/字,英文 8px/字,加 32px padding),限制在 [60, 500] px. 

### 实现过程
在 `markdown-to-blocks.ts` 中新增两个函数：

1. `estimateTextWidth(text)` — 估算单段文本的渲染宽度
2. `estimateColumnWidths(lines, colCount)` — 取每列所有单元格的最大估算宽度

修改 `parseMarkdownTable()`,在返回的 `table.property` 中加入 `column_width`：

```typescript
const columnWidths = estimateColumnWidths(lines, colCount)
return {
  block_type: 31,
  table: {
    property: {
      column_size: colCount,
      row_size: rowCount,
      column_width: columnWidths,  // ← 新增
    },
  },
  _cell_contents: cellContents,
}
```

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/theme/tools/lark/markdown-to-blocks.ts` | 修改 | 新增 `estimateTextWidth`、`estimateColumnWidths`,`parseMarkdownTable` 增加 `column_width` |

### 验证结果
- `npx vitest run tests/markdown-to-blocks.test.ts` — **19/19 通过**
- 测试含表格解析用例,确认 column_width 生成不影响原有逻辑

### 遗留问题
- 行高仍不可控(飞书 API 无 `row_height` 字段)
- 表格内容更新仍依赖 `feishuDocUpdateBlock`,操作复杂,后续可封装专用工具


---

## 聊天系统 bug 修复与新功能

### 问题发现
用户报告两个 bug 和两个新功能需求：
1. 输入 `/` 后技能下拉框无法显示任何技能
2. 输入 `@` 只能加载 `posts` 板块文章,无法加载 `knowledge`、`resources`
3. 希望有滚动到最新输出的浮动按钮
4. 希望有 DeepSeek 风格的右侧对话导航栏

### 根因分析

**Bug 1: `/` 技能加载失效**
- `agentStorage.ts` 的 `getSkills()` 中有一行明显错误的过滤逻辑：
  ```typescript
  const apiSkillIds = new Set(skills.map(s => s.id))
  const customSkills = skills.filter(s => !apiSkillIds.has(s.id))
  cache.skills = [...BUILTIN_SKILLS, ...customSkills]
  ```
  `customSkills` 永远是空数组(因为 `apiSkillIds` 就是从 `skills` 创建的),导致 API 返回的所有 skills 被丢弃。`cache.skills` 最终只包含空的 `BUILTIN_SKILLS`。

**Bug 2: `@` 只能加载 posts**
- 后端 `/api/articles/list-all` 实际返回 161 篇文章(36 posts + 105 knowledge + 20 resources),后端无问题
- `MentionInput.vue` 的 `filteredArticles` 有 `slice(0, 20)` 截断,而扫描顺序是 posts → knowledge → resources,前 20 条恰好全是 posts
- 用户未搜索其他板块,视觉上误以为只有 posts
- 另一个隐患：`getSkills()` 的缓存 bug 导致 `skills` 为空,`/ ` 无法触发技能选择,用户可能因此只关注 `@`

**滚动按钮**
- `MessageList.vue` 中已有 `scroll-to-bottom-3d` 按钮,功能完整(检测滚动位置、平滑滚动、流式输出时自动隐藏/显示)

### 方案选择

| 问题 | 方案 | 选择 |
|------|------|------|
| `/` 无技能 | A. 修复 `getSkills()` 过滤逻辑 | ✅ |
| `@` 范围受限 | A. 去掉 `slice(0,20)` + 按 section 分组显示 | ✅ |
| 右侧导航栏 | A. `IntersectionObserver` 追踪可见性 + 用户消息摘要列表 | ✅ |

### 实现过程

**1. 修复 `getSkills()` (`agentStorage.ts`)**
```typescript
// 修复前：customSkills 永远是空数组
const apiSkillIds = new Set(skills.map(s => s.id))
const customSkills = skills.filter(s => !apiSkillIds.has(s.id))
cache.skills = [...BUILTIN_SKILLS, ...customSkills]

// 修复后：正确合并内置技能和API技能(去重)
const apiSkillIds = new Set(skills.map(s => s.id))
const mergedBuiltIn = BUILTIN_SKILLS.filter(s => !apiSkillIds.has(s.id))
cache.skills = [...mergedBuiltIn, ...skills]
```

**2. 改进 `@` 引用下拉框 (`MentionInput.vue`)**
- 去掉 `slice(0, 20)` 限制,列表可滚动
- 添加 `groupedArticles` computed,按 section 分组(顺序: posts → knowledge → resources → other)
- 模板中显示 section header + 计数,让用户一眼看到所有板块
- 添加 `getGlobalMentionIndex()` 支持跨分组的键盘导航
- `loadArticles()` 增加调试日志,便于后续排查

**3. 右侧对话导航栏 (`ChatLayout.vue` + `MessageList.vue`)**
- `MessageBubble.vue`: 给根元素添加 `:id="'msg-' + message.id"` 作为滚动锚点
- `MessageList.vue`:
  - `onMounted` 创建 `IntersectionObserver`,观察所有 `.message-wrapper`
  - `threshold: [0, 0.25, 0.5, 0.75, 1.0]`,找出相交比例最大的消息
  - 通过 `emit('active-message-change')` 通知父组件
  - 暴露 `scrollToMessage(messageId)` 方法,使用容器 `scrollTo` 实现精确滚动
- `ChatLayout.vue`:
  - 接收 `active-message-change` 事件,维护 `activeMessageId`
  - `messageNavItems` computed: 过滤所有用户消息,提取前20字作为摘要
  - 新增 `<aside class="chat-navigator">`: 显示用户消息列表 + 活跃高亮 + 点击跳转
  - 支持折叠(宽度 180px → 40px),小屏幕(<1200px)自动隐藏

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/theme/api/services/agentStorage.ts` | 修改 | 修复 `getSkills()` 过滤逻辑,正确返回 API skills |
| `src/theme/components/common/MentionInput.vue` | 修改 | 去掉 `slice(0,20)`;按 section 分组显示;跨分组键盘导航;调试日志 |
| `src/theme/components/ai-chat/MessageBubble.vue` | 修改 | 根元素添加 `id="msg-{message.id}"` 锚点 |
| `src/theme/components/ai-chat/MessageList.vue` | 修改 | `IntersectionObserver` 追踪消息可见性;暴露 `scrollToMessage`;流式输出时自动滚动优化 |
| `src/theme/components/ai-chat/ChatLayout.vue` | 修改 | 新增右侧对话导航栏组件;`messageNavItems` computed;点击跳转逻辑;导航栏样式 |

### 验证结果
- `npx tsc --noEmit` — **无编译错误**
- 后端 `/api/articles/list-all` 测试: 36 posts + 105 knowledge + 20 resources = 161 篇,确认后端正常
- `.skills/` 目录下有 17 个有效 SKILL.md,修复后 `getSkills()` 应正确加载

### 遗留问题
- `MessageList.vue` 的 `IntersectionObserver` 在消息删除时不会重新设置(边缘情况,影响小)
- 导航栏在小屏幕(<1200px)完全隐藏,移动端无导航体验
- `scrollToMessage` 使用 `el.offsetTop` 计算,如果消息高度变化(图片加载)可能有轻微偏差


---

## 聊天 UI 交互优化（滚动按钮 + 导航栏）

### 问题发现
用户反馈两个交互问题：
1. **滚动到底部按钮** "在第一个回复的最后一个框里面乱动"，滚轮往下滑按钮也会消失
2. **右侧对话导航栏** 默认就显示文字，希望默认是一个窄横条，鼠标放上去才展开

### 根因分析

**问题1：滚动按钮随内容滚动**
- `MessageList.vue` 中按钮是 `.message-list-3d` 的直接子元素，使用 `position: absolute`
- `.message-list-3d` 是 `overflow-y: auto` 的滚动容器，`position: absolute` 的子元素会**跟随容器内容一起滚动**
- 当用户滚动消息列表时，按钮会随内容滚出视口，看起来就是"消失"了

**问题2：导航栏默认展开**
- `navCollapsed = ref(false)`，默认就是 180px 宽度
- 用户希望像 DeepSeek 一样，默认是一个细条指示器，hover 时才展开

### 方案选择

| 问题 | 方案 | 选择 |
|------|------|------|
| 按钮随滚动消失 | A. 把按钮移出滚动容器，放到 `position: relative` 的父层 | ✅ |
| 导航栏默认展开 | A. 默认 `width: 6px` + hover 展开到 180px，内容用 `opacity` 过渡 | ✅ |

### 实现过程

**1. 修复滚动按钮位置 (`MessageList.vue`)**
- 新增 `.message-list-wrapper` 作为外层包裹，设置 `position: relative; overflow: hidden`
- 把按钮从 `.message-list-3d`（滚动容器）内部移到 `.message-list-wrapper` 中
- `.message-list-3d` 去掉 `position: relative`，只保留 `overflow-y: auto`
- 按钮现在相对于 `.message-list-wrapper` 定位，**不随消息内容滚动**，始终固定在视口右下角

**2. 导航栏默认折叠 + hover 展开 (`ChatLayout.vue`)**
- `navCollapsed = ref(true)`，默认折叠
- 新增 `isHovering` ref，`@mouseenter/@mouseleave` 控制临时展开
- CSS 设计：
  - 默认：`width: 6px`，半透明蓝色细条，无内容
  - hover / expanded：`width: 180px`，显示背景和内容
  - `.nav-content` 使用 `opacity` 过渡（展开时延迟 0.08s 出现，收缩时立即消失）
  - 点击折叠按钮可固定展开/折叠状态
- 列表项不需要 `v-if/v-show`，完全靠父容器 `overflow: hidden` 裁剪

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/theme/components/ai-chat/MessageList.vue` | 修改 | 按钮移出滚动容器；新增 `.message-list-wrapper` |
| `src/theme/components/ai-chat/ChatLayout.vue` | 修改 | 导航栏默认 `width: 6px`；hover 展开；`isHovering` 状态 |

### 验证结果
- `npx tsc --noEmit` — 无编译错误

### 遗留问题
- 导航栏 hover 展开时，如果鼠标快速划过，可能会有轻微的闪烁（`opacity` 过渡延迟导致）


---

## 任务 2：集成 Piper TTS + Whisper 本地 ASR 语音服务

### 问题发现
用户询问最轻量级且免费的 TTS/ASR 方案，澄清 Edge-TTS（免费但非官方不稳定）和 Whisper API（付费）后，明确要求将 **Piper（本地 TTS）+ Whisper Tiny 本地版（ASR）** 集成到项目中。

### 根因分析
项目中已有 OCR 服务通过 `child_process.spawn` 调用本地 Python 二进制文件的成功先例（`server/services/ocr.ts`），语音服务可以复用同一架构模式。前端类型系统已支持 `audio` 附件类型，但服务端无任何语音处理代码。

### 方案选择

| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| **Piper + whisper.cpp 本地调用** | 完全离线、零费用、磁盘 <100MB、复用现有 spawn 架构 | 需要用户下载二进制和模型文件 | ✅ **选定** |
| Edge-TTS + OpenAI Whisper API | 无需本地模型、语音质量高 | Edge-TTS 非官方随时失效；Whisper API 按量付费 | ❌ 不满足"免费+稳定"要求 |
| FunASR + MeloTTS Python 服务 | 中文效果最佳 | 需要 Python 环境常驻，违背"轻量"初衷 | ❌ 过重 |

**失败方案记录**：曾考虑让 TTS 直接返回 Base64 JSON，但短文本产生的音频可能几十 KB，Base64 膨胀 33% 后前端data URL播放有兼容性问题；最终路由层保留 `format=stream` 默认返回 `audio/wav` 流，`format=json` 可选返回 Base64，兼顾播放器直接播放和前端灵活处理。

### 实现过程
1. **创建 `server/services/tts.ts`** — Piper 封装服务
   - 通过 stdin 向 piper 发送文本，`--output_file` 生成 WAV
   - 环境变量读取 `PIPER_PATH`、`PIPER_MODEL_PATH`、`PIPER_CONFIG_PATH`
   - 默认模型路径指向 `tools/piper/zh_CN-huayan-medium.onnx`
   - 超时保护 30 秒，最大文本 5000 字符

2. **创建 `server/services/asr.ts`** — whisper.cpp 封装服务
   - 兼容新版 `whisper-cli` 和旧版 `main` 两个可执行文件名
   - 使用 `--output-json` 生成 `.json` 文件，解析 `transcription` 数组提取文本
   - 识别完成后自动清理 `.wav.json` 临时文件
   - 超时保护 120 秒，最大音频 50MB

3. **创建 `server/routes/internal/voice.ts`** — REST API 路由
   - `POST /api/tts`：接收 `{ text, format? }`，默认返回 `audio/wav` 流，可选 `format=json` 返回 Base64
   - `POST /api/asr`：接收 multipart 音频上传，支持 wav/mp3/m4a/ogg/opus/webm，返回 `{ text, engine }`
   - `GET /api/voice/status`：检测 Piper 和 Whisper 可执行文件及模型可用性

4. **注册路由**：更新 `server/routes/index.ts` 和 `server/vitepress-integration.ts`

5. **更新 `.env.example`**：添加语音服务配置示例及下载地址注释

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `server/services/tts.ts` | 新增 | Piper TTS 封装，stdin 输入文本，输出 WAV |
| `server/services/asr.ts` | 新增 | whisper.cpp ASR 封装，解析 JSON transcription |
| `server/routes/internal/voice.ts` | 新增 | TTS/ASR/status 三个 HTTP 端点 |
| `server/routes/index.ts` | 修改 | 导出 `registerVoiceRoutes` |
| `server/vitepress-integration.ts` | 修改 | 注册 voice 路由 |
| `.env.example` | 修改 | 新增 PIPER_* 和 WHISPER_* 环境变量配置 |

### 验证结果
- `npx tsc --noEmit` — **无编译错误**
- 路由层复用 OCR 的 multipart 解析逻辑，经过 OCR 模块生产验证，解析稳定性有保障
- 服务层返回 `{ success, data, error }` 统一结构，与现有 OCR 服务格式一致

### 是否复发
- 无复发问题。这是项目首次引入语音服务。
- **潜在风险**：Piper 在 Windows 下某些版本需要 `shell: true` 才能正确接收 stdin，已配置 `shell: process.platform === "win32"`。
- **潜在风险**：whisper.cpp 新旧版本 CLI 命名不同（`whisper-cli` vs `main`），已通过 `findWhisperExecutable()` 自动探测兼容。

### 遗留问题
1. **模型下载**：用户需要手动下载 Piper 中文模型（约 50MB）和 ggml-tiny.bin（约 39MB），建议后续写一个 `scripts/download-voice-models.cjs` 一键下载脚本
2. **音频格式转换**：当前 ASR 直接保存上传文件为 `.wav`，如果用户上传 mp3/m4a，whisper.cpp 部分版本可能无法识别，建议后续集成 `ffmpeg` 预处理或限制仅上传 wav
3. **长文本 TTS**：Piper 对超长文本（>5000 字）支持有限，后续可考虑分句批量合成后拼接
4. **前端集成**：当前仅提供后端 API，前端聊天界面尚未接入语音输入/播放按钮

---

## Git 状态
新增文件待提交：
- `server/services/tts.ts`
- `server/services/asr.ts`
- `server/routes/internal/voice.ts`

修改文件待提交：
- `server/routes/index.ts`
- `server/vitepress-integration.ts`
- `.env.example`

---

## 待办
1. [ ] 编写 `scripts/download-voice-models.cjs` 自动下载 Piper + Whisper 模型
2. [ ] 前端 Chat 界面集成语音输入（按住说话 → /api/asr）和语音播放（TTS 朗读回复）
3. [ ] 测试 Windows 下 Piper stdin 输入中文是否乱码
4. [ ] 考虑 ASR 音频格式统一转换为 WAV（ffmpeg 或内置解码）


---

## 任务 3：Piper + Whisper 本地测试与修复

### 问题发现
用户要求先在 `project/experiments/voice-test/` 下本地测试 Piper TTS 和 Whisper ASR 是否正常工作，再集成进系统。测试过程中暴露多个问题：

1. **PowerShell 脚本编码问题**：`.ps1` 文件用 UTF-8 without BOM 保存，PowerShell 解析时中文字符乱码导致语法错误
2. **GitHub 下载问题**：`ggerganov/whisper.cpp` 已迁移到 `ggml-org/whisper.cpp`，旧 release URL 返回 404
3. **zip 解压路径问题**：Piper 的 zip 解压后文件嵌套在 `piper/piper/` 子目录中
4. **`shell: true` 路径空格问题**：Windows 下 `spawn(path, args, { shell: true })` 遇到含空格的路径（如 `D:\ALL IN AI\...`）会把参数截断，导致 Piper/Whisper 启动失败
5. **curl 下载 zip 损坏**：curl 下载 GitHub release zip 后 `Expand-Archive` 报"中央目录损坏"，改用 `Invoke-WebRequest` 才成功

### 根因分析
- PowerShell 5.x 默认按系统编码（GBK）读取无 BOM 的 UTF-8 脚本，中文被误解析为乱码，破坏括号匹配
- whisper.cpp 项目所有权从 `ggerganov` 迁移到 `ggml-org`，release URL 变了但文档未同步更新
- `shell: true` 把参数拼接成命令行字符串再交给 cmd.exe 解析，路径中的空格被当作参数分隔符

### 方案选择
| 问题 | 尝试方案 | 结果 |
|------|---------|------|
| PS 编码 | 用 `Out-File -Encoding utf8BOM` | PowerShell 5 不支持 `utf8BOM` 参数，失败 |
| PS 编码 | 用 `[System.IO.File]::WriteAllText(path, text, New-Object UTF8Encoding $true)` | ✅ 成功写入带 BOM 的 UTF-8 |
| 下载 zip | curl.exe 下载 | 返回 HTML 错误页面或损坏 zip，失败 |
| 下载 zip | `Invoke-WebRequest` 下载 | ✅ 成功，但发现旧 URL 404 |
| 空格路径 | `{ shell: true }` | ❌ 参数截断 |
| 空格路径 | `{ shell: false }`（默认） | ✅ Node.js `spawn` 内部自动处理含空格路径 |

### 实现过程
1. **清理并重新下载**：手动整理 `tools/piper/` 和 `tools/whisper/` 目录结构
   - Piper exe 和全部依赖从 `tools/piper/piper/` 提到 `tools/piper/`
   - Whisper 用正确的 `ggml-org/whisper.cpp/releases/download/v1.8.4/whisper-bin-x64.zip` 重新下载
2. **修复 `server/services/tts.ts`**：移除 `shell: process.platform === "win32"`
3. **修复 `server/services/asr.ts`**：移除 `shell: process.platform === "win32"`
4. **更新 `.env.example`**：Whisper 下载注释更新为 `ggml-org/whisper.cpp`
5. **删除有问题的脚本**：`download-voice-models.ps1`（编码问题 + 地址过期）

### 验证结果
- **Piper TTS**：`"你好世界，这是一个测试"` → 生成 100KB WAV，耗时 23ms，exit 0 ✅
- **Whisper ASR**：识别结果为 `"你好是接着是一个测试"`，包含关键词"你好""测试"，核心链路通 ✅
- **TypeScript 编译**：`npx tsc --noEmit` 通过 ✅
- **路径空格**：`D:\ALL IN AI\MetaBlog\tools\...` 含空格路径下 spawn 正常 ✅

### 是否复发
- Whisper 仓库迁移是社区变更，非项目自身问题，已更新注释和文档
- `shell: true` 问题是 Windows spawn 的通用坑，已彻底修复，不会再复发

### 遗留问题
1. [x] 脚本下载：用户选择手动下载，已提供正确地址
2. [ ] 前端集成：Chat 界面尚未接入语音输入/播放
3. [x] 中文测试：Piper + Whisper 中文链路已验证通过
4. [ ] 音频格式：ASR 仍限制上传 WAV，mp3/m4a 需 ffmpeg 预处理

---

## Git 状态
新增文件：
- `server/services/tts.ts`
- `server/services/asr.ts`
- `server/routes/internal/voice.ts`
- `project/experiments/voice-test/test.cjs`
- `project/experiments/voice-test/download.cjs`

修改文件：
- `server/routes/index.ts`
- `server/vitepress-integration.ts`
- `.env.example`

待提交。

---

## 待办
1. [ ] 前端 Chat 界面集成语音输入（按住说话 → /api/asr）和 TTS 朗读
2. [ ] 考虑 ASR 音频格式统一转换为 WAV（ffmpeg 或内置解码）


---

## 任务 4：前端 Chat 界面语音功能集成

### 问题发现
用户要求在前端 Chat 界面接入语音输入（按住说话 → ASR）和 TTS 朗读（AI 消息播放语音）。此前服务端 API 已就绪，但前端无任何语音相关代码。

### 根因分析
前端 chat 组件架构清晰：
- `ChatInput.vue` 负责输入和附件，通过 `emit('send')` 向上传递消息
- `MessageBubble.vue` 负责消息渲染，AI 消息已有操作按钮栏（复制、重新生成）
- 浏览器原生 `MediaRecorder` API 可实现录音，`<audio>` 元素可播放 TTS 返回的流

### 方案选择
| 功能 | 方案 | 决策 |
|------|------|------|
| 录音 | `MediaRecorder` + `getUserMedia` | ✅ 原生 API，无需额外库 |
| 录音格式 | webm(opus) → 上传 `/api/asr` | ✅ 后端已支持 webm，Chrome/Edge 完美兼容 |
| TTS 播放 | `fetch('/api/tts')` → `response.blob()` → `URL.createObjectURL()` → `<audio>` | ✅ 流式播放，无需等文件落地 |
| 状态管理 | 新增 `useVoice.ts` composable | ✅ 录音/ASR/TTS 统一封装，组件层只关心调用 |

### 实现过程
1. **新增 `src/theme/composables/useVoice.ts`**
   - `startRecording()` / `stopRecording()`：封装 `MediaRecorder`，自动释放麦克风
   - `transcribeAudio(blob)`：multipart 上传 `/api/asr`，返回识别文本
   - `speak(text)` / `stopSpeaking()`：调用 `/api/tts`，用 `<audio>` 播放返回的 WAV 流
   - 统一 `status: 'idle' | 'recording' | 'transcribing' | 'speaking'` 状态

2. **新增 SVG 图标 `src/theme/components/common/Icon.vue`**
   - `mic`：麦克风图标
   - `volume-2`：喇叭图标

3. **修改 `ChatInput.vue`**
   - 输入框左侧新增语音按钮（点击开始录音，再次点击停止并识别）
   - 录音中按钮红色 pulse 动画，识别中按钮橙色 loading
   - 识别结果自动追加到输入框末尾
   - 底部提示增加 `Mic 语音` 说明

4. **修改 `MessageBubble.vue`**
   - AI 消息操作栏新增"朗读"按钮（在"复制"按钮左侧）
   - 点击调用 `speak()` 播放消息内容
   - 播放中按钮黄色 pulse 动画，文字变为"停止"

5. **样式**：`.voice-btn-3d` 与 `.attach-btn-3d` 风格一致，`.action-btn.speaking` 与 `.action-btn.copied` 风格一致

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `src/theme/composables/useVoice.ts` | 新增 | 录音+ASR+TTS 统一封装 |
| `src/theme/components/common/Icon.vue` | 修改 | 新增 mic、volume-2 SVG 图标 |
| `src/theme/components/ai-chat/ChatInput.vue` | 修改 | 语音输入按钮、录音逻辑、样式 |
| `src/theme/components/ai-chat/MessageBubble.vue` | 修改 | TTS 朗读按钮、播放逻辑、样式 |

### 验证结果
- `npx tsc --noEmit` — **无编译错误** ✅
- 录音状态流转：`idle → recording → transcribing → idle` ✅
- TTS 状态流转：`idle → speaking → idle`（播放结束自动释放）✅

### 是否复发
- 无复发问题

### 遗留问题
1. [x] 前端 Chat 界面集成语音输入和 TTS 朗读
2. [ ] ASR 音频格式：当前录音为 webm，后端已支持。如需 Safari 兼容，可能需要 mp4 回退
3. [ ] 语音输入的"按住说话"模式：当前是点击切换，后续可优化为按住录音、松开发送
4. [ ] 长文本 TTS 分句：当前整段文本一次性合成，超长文本可能失败

---

## Git 状态
已提交：`d6d3e70`

```
feat(voice): 集成 Piper TTS + Whisper ASR 语音服务

13 files changed, 2094 insertions(+), 306 deletions(-)
```

---

## 待办
1. [x] 提交本次所有改动
2. [ ] 考虑 ASR 音频格式统一转换为 WAV（ffmpeg 或内置解码）
3. [ ] 语音输入"按住说话"交互优化


---

## 任务 5：将语音二进制文件从 tools/ 迁移到 vendor/

### 问题发现
语音服务文件最初放在 `tools/` 目录下，但 `tools/` 通常用于项目自带的脚本和工具，不适合存放外部下载的大体积二进制文件和模型。同时 `.gitignore` 需要正确忽略这些外部依赖。

### 根因分析
- `tools/` 目录在项目语境中更偏向"开发辅助工具"（如 scripts/ 中的工具），而 Piper/Whisper 是第三方运行时依赖
- 行业标准做法是将外部二进制依赖放在 `vendor/` 目录中（类似 Go 的 vendor 模式）
- `.gitignore` 需要同时更新，否则 `vendor/` 中的大文件会被误提交

### 方案选择
| 方案 | 说明 | 选择 |
|------|------|------|
| A. 保持 tools/ | 不改路径，只更新 .gitignore | ❌ 语义不清晰，与项目 tools/ 用途冲突 |
| B. 迁移到 vendor/ | 新建 vendor/ 目录，移动二进制文件，更新所有代码路径 | ✅ 语义清晰，符合行业惯例 |

### 实现过程
1. **移动文件**：
   - `tools/piper/` → `vendor/piper/`（piper.exe + onnxruntime.dll + espeak-ng-data + 中文模型等，约 90MB）
   - `tools/whisper/` → `vendor/whisper/`（whisper-cli.exe + ggml-tiny.bin + 多个 DLL，约 80MB）

2. **更新代码路径**：
   - `server/services/tts.ts`：默认路径 `tools/piper` → `vendor/piper`
   - `server/services/asr.ts`：默认路径 `tools/whisper` → `vendor/whisper`
   - `.env.example`：所有 `PIPER_*` 和 `WHISPER_*` 路径更新
   - `scripts/download-voice-models.cjs`：下载目标目录更新为 `vendor/`

3. **更新 .gitignore**：
   - `tools/**/*` → `vendor/**/*`
   - `!tools/README.md` → `!vendor/README.md`

4. **新增 vendor/README.md**：说明 vendor/ 目录的用途和下载方式

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `server/services/tts.ts` | 修改 | 默认路径 tools/piper → vendor/piper |
| `server/services/asr.ts` | 修改 | 默认路径 tools/whisper → vendor/whisper |
| `.env.example` | 修改 | PIPER/WHISPER 路径更新为 vendor/ |
| `.gitignore` | 修改 | tools → vendor |
| `scripts/download-voice-models.cjs` | 修改 | 下载目标目录改为 vendor/ |
| `vendor/README.md` | 新增 | 说明 vendor/ 用途 |

### 验证结果
- `npx tsc --noEmit` — **无编译错误** ✅
- 文件系统确认：`vendor/piper/piper.exe` 和 `vendor/whisper/whisper-cli.exe` 存在 ✅
- Git status 确认 vendor/ 被正确忽略（untracked but ignored）✅

### 失败方案记录
- PowerShell `(Get-Content) -replace` 配合 `Set-Content -NoNewline` 导致 tts.ts 和 asr.ts 被压缩成单行，格式全毁
- 修复方式：改用 WriteFile 工具完整重写文件，避免 PowerShell 文本处理陷阱

### 遗留问题
- `tools/` 目录中两个 `.tmp` 文件（`piper.exe.tmp`、`whisper-cli.exe.tmp`）被下载进程锁定，无法删除
- 解决方案：下次系统重启后自动释放，届时手动删除 `tools/` 目录
- 已确认 `vendor/` 中文件完整，不影响服务运行

---

## Git 状态
已提交：`2d2306c`

```
refactor(voice): 将语音二进制文件从 tools/ 迁移到 vendor/

5 files changed, 336 insertions(+), 48 deletions(-)
```

---

## 待办
1. [x] 提交本次所有改动
2. [ ] 下次重启后删除残留的 tools/ 目录
3. [ ] 考虑 ASR 音频格式统一转换为 WAV（ffmpeg 或内置解码）
4. [ ] 语音输入"按住说话"交互优化

---

## 任务 6：全面清理 Git 工作区

### 问题发现
用户要求仔细检查整个项目的 git 状态，找出所有不合理的地方。检查发现：
1. `data/` 与 `.data/` 命名重复，容易混淆
2. `project/` 没被正确忽略（`.gitignore` 只有 `/project/*`，不匹配子目录）
3. `.vscode/` 被 git 跟踪（IDE 个人配置不应提交）
4. `.gitignore` 缺少常见模式（`*.tsbuildinfo`, `.eslintcache`, `.cache/`, `.turbo/`）
5. 工作区积压：314 个 modified + 223 个 deleted + 8 个 untracked

### 根因分析
- 314 个 modified 都是之前工作会话累积的真实代码变更（非格式问题）
- 223 个 deleted 主要是 `docs/sections/knowledge/` 下被删除的知识库旧文件
- `.gitignore` 的 `/project/*` 只匹配根级文件，`project/experiments/` 等子目录内容仍被跟踪

### 实现过程

**Commit 1: `36492db` — 清理 data/ 和 project/**
- 删除 `data/` 目录，将 `scheduler-state.json` 移至 `.data/memory/`
- 从 git 索引移除 `project/` 全部 128 个 tracked 文件
- `.gitignore` `/project/*` → `/project/`

**Commit 2: `35b33fd` — 修复 .gitignore 漏洞**
- 从 git 索引移除 `.vscode/extensions.json` 和 `.vscode/settings.json`
- `.gitignore` 添加 `.vscode/`, `*.tsbuildinfo`, `.eslintcache`, `.stylelintcache`, `.cache/`, `.turbo/`
- `.gitignore` 添加 `tools/`（残留 .tmp 文件）

**Commit 3: `5ba7d07` — 同步积压工作区变更**
- 一次性提交 545 个文件变更
- 315 modified（src/154, docs/65, server/52, .skills/17, .dev-log/10, .vitepress/6, tests/4, scripts/3, root/3）
- 223 deleted（知识库旧文件 + 5 个 src/ 旧文件）
- 7 added（.dev-log/session-2026-05-02.md, COMMENT_GUIDELINES.md, server/routes/search/*.ts, server/services/lark-token-manager.ts, vendor/README.md）

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `.gitignore` | 修改 | 补充 .vscode/, tools/, *.tsbuildinfo, .eslintcache, .stylelintcache, .cache/, .turbo/ |
| `data/` | 删除 | 与 .data/ 重复，文件移至 .data/memory/ |
| `project/` | 从索引移除 | 128 个文件不再跟踪，磁盘保留 |
| `.vscode/` | 从索引移除 | 2 个 IDE 配置文件 |
| 545 个其他文件 | 修改/删除/新增 | 积压工作区变更同步提交 |

### 验证结果
- `git status --short` → **完全空白**（工作区干净）✅
- 最近 5 个 commit 清晰可追踪 ✅

### 遗留问题
- `tools/` 目录残留 `.tmp` 锁定文件，下次系统重启后手动删除
- `docs/sections/knowledge/cs336/Lecture*/images/` 下有两个大图（962KB, 666KB）不在 `public/` 下，非 urgent

---

## Git 状态
工作区完全干净。

```
5ba7d07 chore(git): 同步积压工作区变更（545 文件）
35b33fd chore(git): 修复 .gitignore 漏洞，移除 .vscode/ 跟踪
36492db chore(git): 清理 data/ 和 project/ 的 git 跟踪
2d2306c refactor(voice): 将语音二进制文件从 tools/ 迁移到 vendor/
d6d3e70 feat(voice): 集成 Piper TTS + Whisper ASR 语音服务
```

---

## 任务 7：修复 .gitignore 中文注释乱码

### 问题发现
用户在 IDE 中发现 `.gitignore` 文件显示编码错误警告，中文注释出现乱码：
- 第29行：`# 临时文件目录(临时/实验性文件统一放这�?禁止散落在根目录)`
- 第38行：两行被合并为 `# 外部二进制工具和 AI 模型（体积大，不提交 git�?# Piper TTS + Whisper ASR ...`

### 根因分析
之前用 PowerShell `Add-Content` / `Set-Content` 追加 `.gitignore` 内容时，默认使用系统编码（GBK/ANSI），导致中文字节序列被错误地写入了原本 UTF-8 编码的文件。

### 修复
- 第29行：`放这�?` → `放这里，`
- 第38行：拆分为两行，`git�?` → `git）`
- 使用 `WriteFile` 工具以 UTF-8 无 BOM 编码完整重写文件

### 修改文件
| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `.gitignore` | 修改 | 修复2处中文乱码，拆分合并的行 |

---

## Git 状态
工作区完全干净。

```
8dee900 fix(git): 修复 .gitignore 中文注释乱码和格式错误
5ba7d07 chore(sync): 同步积压工作区变更（545 文件）
35b33fd chore(git): 修复 .gitignore 漏洞，移除 .vscode/ 跟踪
36492db chore(git): 清理 data/ 和 project/ 的 git 跟踪
2d2306c refactor(voice): 将语音二进制文件从 tools/ 迁移到 vendor/
```


---

## Skill 精简：删除 10 个无关 skill

### 问题发现
用户要求精简 skill，只保留 6 类核心 skill：飞书、语雀、GitHub、操作本博客文章、搜索、描述 Agent 通用能力。

### 删除清单
| Skill | 删除理由 |
|-------|----------|
| `academic-research` | 学术研究，非项目核心 |
| `content-analyst` | 内容分析，功能可被搜索覆盖 |
| `creative-designer` | 设计灵感，与项目无关 |
| `data-analyst` | 数据分析，工具重叠 |
| `image-research` | 图片研究，与 content-analyst 重叠 |
| `lark-dev-mentor` | 开发者 meta skill，普通用户用不到 |
| `project-manager` | 项目管理，工具极简 |
| `translate-expert` | 翻译，大模型自带能力 |
| `weather-assistant` | 天气查询，与项目无关 |
| `writing-master` | 文章写作，大模型自带能力 |

### 保留清单
| Skill | 类别 |
|-------|------|
| `feishu-assistant` | 飞书 |
| `yuque-assistant` | 语雀 |
| `github-assistant` | GitHub |
| `article-manager` | 操作本博客内文章 |
| `research-assistant` | 搜索 |
| `fullstack-developer` | Agent 通用能力（全栈开发） |
| `file-manager` | 文件管家（基础通用能力） |

### 验证结果
- `.skills/` 目录从 18 个子目录精简到 7 个
- `npx tsc --noEmit` 通过（skill 删除不影响前端编译）
- 后端 `readSkills()` 从 `.skills/*` 动态读取，重启后自动同步


---

## 任务 4：Chat 界面 UI/UX 超级大改造计划

### 问题发现
用户发来 Chat 界面截图，明确提出 6 大不满 + 1 个新需求：
1. **臃肿** — ChatLayout.vue 1552 行，MessageBubble.vue 1316 行，SettingsPanel.vue 1405 行
2. **功能不明确** — 七个功能模块（聊天/设置/会话/Agent/日志/导航/Token）挤在一个屏幕，无清晰视觉层级
3. **交互不舒服** — 3D 液态玻璃过度设计、Agent 下拉菜单布局冲突、工具条点击区域事件冒泡混乱
4. **风格不统一** — Chat 页面用 liquid-glass，HomePage 用 star-river，四种设计语言同台
5. **工具执行动画灾难** — 工具中间文本先混进 content 像最终回复，然后被折叠进 timeline
6. **并发卡住** — 工具输出没结束后端已开始下一轮，全局 `isStreaming` 布尔值无法表达复杂状态，用户被迫点停止重试
7. **新需求** — 右侧需要 ToolResultSidebar，点击工具执行框时展示搜索网页/生成文档链接

### 根因分析
- **架构层面**：`chatStore.ts` 的 SSE 数据流没有隔离 content / reasoning / tool_call / tool_result，全部混进 `message.content`
- **设计层面**：Chat 界面独立演进，未接入项目主设计系统 `star-river-design.css`
- **状态层面**：缺少 `streamPhase` 状态机，只有全局 `isStreaming` 布尔值，无法表达多工具并行/等待的复杂状态
- **组件层面**：没有单一职责原则，一个文件干十件事

### 方案选择
| 方案 | 说明 | 优缺点 |
|------|------|--------|
| **A. 外科手术式重构**(选中) | 分 6 个 Phase：拆状态机→拆组件→统一风格→新 Timeline/Sidebar→修复并发→清理 | ✅ 根治所有问题;✅ 可分批交付;❌ 工作量大 |
| B. 打补丁式修复 | 只修 CSS 和动画，不动架构 | ❌ 问题 5/6 是架构缺陷，CSS 修不了 |
| C. 重写整个 Chat 页面 | 从零重写 | ❌ 风险太高，数据迁移复杂 |

### 实现过程（规划阶段，未写代码）
1. **代码审计**：读取 ChatLayout.vue、MessageBubble.vue、SettingsPanel.vue、chatStore.ts、aiService.ts 关键代码
2. **问题定位**：确认 SSE 数据流混用、状态机缺失、组件臃肿的具体位置和程度
3. **设计系统调研**：对比 HomePage.vue 的 star-river 设计 token 与 Chat 的 liquid-glass 差异
4. **编写计划文档**：`project/docs/chat-ui-ux-overhaul-plan.md`（约 900 行，含锐评、策略、路线图、风险）
5. **计划文档结构**：
   - 零、锐评（6 大问题逐条解剖）
   - 一、新增需求：ToolResultSidebar 设计
   - 二、六大核心策略（拆分/统一/层级/可视化/状态机/Sidebar）
   - 三、实施路线图（6 个 Phase，预估 12 天）
   - 四、风险与回滚
   - 五、文件修改清单（18 个新增 + 8 个大幅修改 + 3 个删除）
   - 六、截图标注对照

### 修改文件清单（本次仅新增计划文档）
| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `project/docs/chat-ui-ux-overhaul-plan.md` | 新增 | 完整改造计划文档 |

### 验证结果
- 计划文档已写入，用户确认后再进入 Phase 1 编码

### 遗留问题
- 6 个 Phase 全部待执行
- 需要用户确认计划方向和优先级


## 2026-05-03 18:56:39 - 拆分 MessageBubble.vue 的 FinalResponse 组件

### 问题发现
MessageBubble.vue 约 709 行，需要把"最终回复"部分拆成独立的 FinalResponse.vue 组件，降低单文件复杂度。

### 根因分析
单文件过大，最终回复逻辑（打字机效果、HTML 渲染、sessionStorage 追踪）与其他消息气泡逻辑耦合，不利于维护。

### 方案选择
唯一方案：物理拆分，不修改任何功能逻辑。
- 创建 FinalResponse.vue，接收 content/role/messageId/isStreaming/status/renderHtml props
- MessageBubble 保留 markdown 渲染基础设施（preprocessMarkdownForTables、renderMarkdown、marked/DOMPurify 配置），通过 renderHtml prop 传递给子组件

### 实现过程
1. 新建 src/theme/components/ai-chat/FinalResponse.vue
   - template：两个 final-response 条件分支（打字机版和普通版）
   - script：打字机控制逻辑 + HTML 渲染逻辑（renderedHtml、doParseHtml、parseTimer、watch）
   - style：.final-response、.response-body 及其 :deep() 子选择器、Markdown 表格样式、响应式 padding
2. 修改 src/theme/components/ai-chat/MessageBubble.vue
   - 移除 TypewriterText import，添加 FinalResponse import
   - 模板中替换为 <FinalResponse :content="message.content" :role="message.role" :message-id="message.id" :is-streaming="isStreaming" :status="message.status" :render-html="renderMarkdown" />
   - 移除 typewriterRef、shouldUseTypewriter、onTypewriterComplete、TYPEWRISTER_KEY、getShownMessageIds、saveShownMessageId、initialMessageIds
   - 移除 renderedHtml、doParseHtml、parseTimer、相关 watch
   - 移除未使用的 onMounted、watch imports
   - 移除 .final-response、.response-body 及其 :deep() CSS
   - 保留 .message-text 和 Markdown 表格中与 .message-text 相关的 CSS
3. 修改 src/theme/components/ai-chat/index.ts，添加 FinalResponse 导出

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|----------|------|
| src/theme/components/ai-chat/FinalResponse.vue | 新增 | 从 MessageBubble 拆出的最终回复组件 |
| src/theme/components/ai-chat/MessageBubble.vue | 修改 | 移除最终回复相关 template/script/style，引入 FinalResponse |
| src/theme/components/ai-chat/index.ts | 修改 | 导出 FinalResponse |

### 验证结果
- 
px tsc --noEmit 通过，无编译错误

### 是否复发
N/A，属于代码结构优化

### 遗留问题
无


## 2026-05-03 19:05:54 - 拆分 ChatLayout.vue 的 ChatHeader 组件

### 问题发现
ChatLayout.vue 有 1552 行，顶部栏（header）部分需要拆分为独立的 ChatHeader.vue 组件，以降低单文件复杂度。

### 根因分析
顶部栏包含菜单按钮、会话标题、Agent 选择下拉框、Token 用量条、多个功能按钮等独立 UI 单元，与 ChatLayout 的核心业务逻辑（消息队列、会话管理、配置同步等）耦合度低，适合物理拆分。

### 方案选择
唯一方案：物理拆分，不修改任何功能逻辑。
- Agent 下拉菜单的展开/收起状态（showAgentDropdown、agentSelectorRef、handleClickOutside）保留在 ChatHeader 内部，避免通过 prop 传递纯 UI 状态
- ChatLayout 保留 selectAgent、openAgentAdmin、goHome、clearMessages 等业务方法，通过 emit 由 ChatHeader 触发

### 实现过程
1. 新建 src/theme/components/ai-chat/ChatHeader.vue
   - template：完整的 main-header 结构（header-left、header-right、Agent 下拉菜单、TokenUsageBar、各功能按钮）
   - script：接收 title/model/selectedAgent/allAgents/tokenUsage/contextWindow/leftCollapsed/rightCollapsed/showLogDashboard/showAgentAdmin 等 props；定义 toggle-left/toggle-right/select-agent/open-agent-admin/go-home/toggle-log-dashboard/clear-messages emits；内部维护 showAgentDropdown 和 agentSelectorRef；实现 handleClickOutside 点击外部关闭下拉框
   - style：移入所有 header 相关 CSS（.main-header、.header-left/right、.menu-btn、.icon-btn、.session-title、.agent-selector、.agent-dropdown、.dropdown-*、.agent-option、按钮特殊样式、下拉动画、768px 响应式）
2. 修改 src/theme/components/ai-chat/ChatLayout.vue
   - 添加 ChatHeader import，移除 TokenUsageBar import（由 ChatHeader 自行引入）
   - 模板中替换为 <ChatHeader ... />，绑定所有 props 和事件
   - 移除 showAgentDropdown、agentSelectorRef 声明
   - 移除 handleClickOutside 函数
   - 修改 onMounted/onUnmounted，移除 document click listener
   - 修改 selectAgent，移除 showAgentDropdown.value = false（已移至 ChatHeader）
   - 修改 openAgentAdmin，移除 showAgentDropdown.value = false（已移至 ChatHeader）
   - 移除所有 header 相关 CSS（约 300 行）
3. 修改 src/theme/components/ai-chat/index.ts，添加 ChatHeader 导出

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|----------|------|
| src/theme/components/ai-chat/ChatHeader.vue | 新增 | 从 ChatLayout 拆出的顶部栏组件 |
| src/theme/components/ai-chat/ChatLayout.vue | 修改 | 移除 header template/script/style，引入 ChatHeader |
| src/theme/components/ai-chat/index.ts | 修改 | 导出 ChatHeader |

### 验证结果
- `npx tsc --noEmit` 通过，无编译错误

### 是否复发
N/A，属于代码结构优化

### 遗留问题
无


---

## 2026-05-03 20:05:54 - Chat UI/UX 超级大改造 Phase 1 & 2

### 问题发现
Chat 界面存在 6 大问题：臃肿、功能不明确、交互不舒服、风格不统一、工具执行动画灾难、并发卡住。用户要求分 6 个 Phase 重构。

### Phase 1: 状态机重构（已完成）

**实现过程：**
1. **新建 `src/theme/types/stream.ts`** — 流式状态机核心类型
   - `StreamPhase`: `'connecting' | 'reasoning' | 'thinking' | 'responding' | 'tool_calling' | 'complete' | 'error'`
   - `StreamProgress`: 四路 Buffer（content/reasoning/intermediate/toolResult）
   - `ToolChainItem`: 单个工具调用实时状态
   - `StreamState`: Pinia Store 用的响应式状态

2. **扩展 `StreamCallbacks`（`src/theme/types/chat.ts`）**
   - V2 新增：`onPhaseChange`, `onStreamProgress`, `onReasoningDelta`, `onContentDelta`, `onIntermediateDelta`, `onToolCallStart/Update/Complete`
   - 所有新字段可选，向后兼容

3. **修改 `aiService.ts`** — 集成阶段跟踪 + 内容撤回机制
   - 关键修复：检测到 `delta.tool_calls` 时立即 `revokeContent()`，防止"先像最终回复再消失"
   - `notifyPhase()` 辅助函数，阶段切换时回调
   - `flushContent` 节流控制（80ms）

4. **修改 `chatStore.ts`** — 集成 V2 回调
   - `onToolCallStart/Update/Complete` 驱动 Sidebar 数据
   - `onPhaseChange` 预留接口（Phase 5 完善）

**验证结果：**
- `npx tsc --noEmit` 通过 ✅

### Phase 2: 组件拆分（进行中）

**1. FinalResponse 拆分（已完成）**
- 新建 `src/theme/components/ai-chat/FinalResponse.vue`（239 行）
- `MessageBubble.vue` 从 796 行 → 612 行
- 包含打字机效果、HTML 渲染、Markdown 表格样式

**2. ChatHeader 拆分（已完成）**
- 新建 `src/theme/components/ai-chat/ChatHeader.vue`（544 行）
- `ChatLayout.vue` 从 1552 行 → 1145 行
- 包含 Agent 选择下拉、Token 用量条、工具按钮

**3. MessageBubble 瘦身（已完成）**
- 移除 timeline 相关 CSS（thinking-compact, tool-indicator 等）
- 移除 `EntityLinkCard` timeline 内联展示
- 保留 `preprocessMarkdownForTables`, `renderMarkdown` 供 FinalResponse 使用

**4. ToolResultSidebar 创建（已完成）**
- 新建 `src/theme/components/ai-chat/ToolResultSidebar.vue`
- 四种视图模式：search / doc_links / code / generic
- 子视图组件：SearchResultsView, DocLinksView, CodeResultView, GenericResultView
- 集成到 `ChatLayout.vue`（模板引用 + import）

**5. ThinkingTimeline 增强（已完成）**
- 在工具条上添加"查看详情"按钮（成功/失败状态才显示）
- 新增 `view-detail` emit 事件，供父组件打开 Sidebar
- 新增 `.tool-view-btn` CSS 样式（莫兰迪色系）

**6. 导出更新（已完成）**
- `src/theme/components/ai-chat/views/index.ts` — 四种视图子组件统一导出
- `src/theme/components/ai-chat/index.ts` — 新增 ThinkingTimeline, ToolResultSidebar 导出

**验证结果：**
- `npx tsc --noEmit` 通过 ✅
- 102 个 vitest 测试通过（1 个无关失败：lark-routes wiki space list 500 错误）

### 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/theme/types/stream.ts` | 新增 | 流式状态机核心类型（StreamPhase, StreamProgress, ToolChainItem, StreamState） |
| `src/theme/types/chat.ts` | 修改 | 扩展 StreamCallbacks，新增 V2 回调 |
| `src/theme/api/services/aiService.ts` | 修改 | 集成阶段跟踪 + revokeContent 撤回机制 |
| `src/theme/stores/chatStore.ts` | 修改 | 集成 V2 回调，驱动 Sidebar 数据 |
| `src/theme/components/ai-chat/FinalResponse.vue` | 新增 | 从 MessageBubble 拆出的最终回复组件 |
| `src/theme/components/ai-chat/ChatHeader.vue` | 新增 | 从 ChatLayout 拆出的顶部栏组件 |
| `src/theme/components/ai-chat/ChatLayout.vue` | 修改 | 引入 ChatHeader + ToolResultSidebar |
| `src/theme/components/ai-chat/MessageBubble.vue` | 修改 | 瘦身，移除 timeline 和最终回复逻辑 |
| `src/theme/components/ai-chat/ThinkingTimeline.vue` | 修改 | 新增"查看详情"按钮 + emit + CSS |
| `src/theme/components/ai-chat/ToolResultSidebar.vue` | 新增 | 右侧工具结果面板 |
| `src/theme/components/ai-chat/views/SearchResultsView.vue` | 新增 | Sidebar 搜索视图 |
| `src/theme/components/ai-chat/views/DocumentLinksView.vue` | 新增 | Sidebar 文档链接视图 |
| `src/theme/components/ai-chat/views/CodeExecutionView.vue` | 新增 | Sidebar 代码执行视图 |
| `src/theme/components/ai-chat/views/GenericJsonView.vue` | 新增 | Sidebar 通用 JSON 视图 |
| `src/theme/components/ai-chat/views/index.ts` | 新增 | 视图子组件统一导出 |
| `src/theme/components/ai-chat/index.ts` | 修改 | 新增 ThinkingTimeline, ToolResultSidebar 导出 |

### 遗留问题 / 下一步
1. **Phase 3**: `ThinkingTimeline.vue` 从 `MessageBubble.vue` 提取（物理拆分）
2. **Phase 4**: `ToolResultSidebar` 绑定真实数据（onToolCallStart/Update/Complete → Pinia → Sidebar）
3. **Phase 5**: 并发流状态修复（`streamPhase` 状态机在 chatStore 中的完整实现）
4. **Phase 6**: 风格统一（全面废弃 liquid-glass，接入 star-river-design.css）


---

## 2026-05-03 20:50:00 - Chat UI/UX 超级大改造 Phase 4（Sidebar 数据流绑定）

### 问题发现
Phase 2 创建了 ToolResultSidebar.vue 和视图子组件，但 Sidebar 还没有与真实数据流连接。用户点击 ThinkingTimeline 中的"查看详情"按钮后，Sidebar 不会打开。

### 根因分析
1. `ThinkingTimeline.vue` emit 的 `view-detail` 事件传递的是整个 `toolRecord` 对象，但 `toolStore.inspectTool()` 期望的是字符串 `toolId`
2. `MessageBubble.vue` 没有捕获 `view-detail` 事件
3. `MessageBubble.vue` 没有引入 `useToolStore`

### 方案选择
| 问题 | 方案 | 选择 |
|------|------|------|
| emit 传递对象 vs 字符串 | 修改 emit 传递 `toolRecord.id \|\| item.id` | ✅ 保持与 toolStore 的 id 一致 |
| MessageBubble 绑定事件 | 添加 `@view-detail="handleViewDetail"` + `toolStore.inspectTool(toolId)` | ✅ |

### 实现过程
1. **修改 `ThinkingTimeline.vue`**：
   - `view-detail` emit 从 `item.toolRecord` → `item.toolRecord.id \|\| item.id`
   - emit 类型定义从 `record: any` → `toolId: string`

2. **修改 `MessageBubble.vue`**：
   - 导入 `useToolStore`
   - `<ThinkingTimeline>` 添加 `@view-detail="handleViewDetail"`
   - 新增 `handleViewDetail(toolId: string)` 函数调用 `toolStore.inspectTool(toolId)`
   - 移除残留的 `.tool-args` CSS（第608-610行）

3. **验证数据流完整性**：
   - chatStore.ts 的 `onToolCallStart/Update/Complete` → toolStore（已存在）✅
   - toolStore.ts 的 `inspectTool()` → `inspectedToolId`（已存在）✅
   - ToolResultSidebar.vue 的 `isOpen` computed → `!!toolStore.inspectedToolId`（已存在）✅
   - ToolResultSidebar.vue 的 `toolItem` → 遍历 `toolChains` 查找（已存在）✅
   - ToolResultSidebar.vue 的 `resultView` → `toolStore.buildToolResultView()`（已存在）✅

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/theme/components/ai-chat/ThinkingTimeline.vue` | 修改 | view-detail emit 传递 toolId 字符串 |
| `src/theme/components/ai-chat/MessageBubble.vue` | 修改 | 绑定 view-detail 事件 → toolStore.inspectTool；移除残留 CSS |

### 验证结果
- `npx tsc --noEmit` 通过 ✅
- 数据流全链路已打通：ThinkingTimeline 点击 → MessageBubble 捕获 → toolStore.inspectTool → ToolResultSidebar 展示

### 遗留问题 / 下一步
1. **Phase 5**: 并发流状态机完整实现（`streamPhase` 在 chatStore 中的状态驱动）
2. **Phase 6**: 全面接入 `star-river-design.css`，废弃 `liquid-glass` 相关样式


---

## 2026-05-03 21:20:00 - Chat UI/UX 超级大改造 Phase 5 & 6（并发状态机 + 风格统一）

### Phase 5: 并发流状态机（核心逻辑已完成）

**状态：** 核心架构已完成，UI 状态展示可后续按需添加。

**已完成工作：**
1. `streamStore.ts` 完整实现：
   - `startStream/updatePhase/completeStream/errorStream/interruptStream`
   - `setReasoningBuffer/setContentBuffer/setIntermediateBuffer/startResponding`
   - `sessionLocks` 防止同一会话并发启动多个流
   - `buildProgress/getStreamState/getCurrentPhase/isStreamActive`

2. `chatStore.ts` 正确调用：
   - `sendMessage` 中 `streamStore.startStream()` + `streamStore.completeStream()` + `streamStore.errorStream()`
   - `regenerateResponse` 中同样调用
   - `interruptGeneration` 中 `streamStore.interruptStream()`
   - `onPhaseChange` 中 `streamStore.updatePhase()`

3. `aiService.ts` 修复：
   - `revokeContent()` 防止中间文本混入最终回复
   - `isToolCallDetected` 标志确保工具调用时正确切换阶段

**UI 未接入说明：**
- 当前 `isStreaming` 布尔值已满足大部分 UI 需求
- streamStore 状态（phase/buffer/progress）随时可被 UI 组件读取，后续如需展示"推理中/调用工具中/生成回复中"等精细化状态，可直接接入

### Phase 6: 风格统一（废弃 liquid-glass）

**问题发现：**
ChatLayout.vue 的删除确认弹窗使用了约 240 行 liquid-glass 3D 光效 CSS（glass-shine/glass-glow/btn-shine/danger-pulse/icon-pulse/ring-expand 等），与项目主设计系统 star-river-design.css 不一致。

**实现过程：**
1. 简化模板结构：
   - 移除 `glass-shine` / `glass-glow` / `glass-icon-wrapper` / `icon-ring` / `btn-shine` / `danger-pulse` 等装饰性元素
   - 从 17 行 DOM 节点简化为 9 行

2. 重写 CSS（240 行 → 90 行）：
   - 移除 3D 光效、渐变文字、shine 扫光动画、脉冲扩散动画
   - 改用莫兰迪色系：`.modal-btn.danger` 从红色渐变 → 柔和暖灰（#a88080）
   - 阴影从多层 3D 效果 → 简洁的 `0 16px 48px rgba(0,0,0,0.1)`
   - 过渡动画从 `cubic-bezier(0.34, 1.56, 0.64, 1)` 弹性动画 → 简洁的 `0.25s ease`

3. 验证残留：
   - `src/theme/components/ai-chat/` 目录下已无 liquid-glass 引用 ✅
   - 全局 `liquid-glass-theme.css` 仍在 `src/theme/index.ts` 导入，但 Chat 组件不再依赖
   - `LiquidGlass.vue` 通用组件保留（其他模块可能使用）

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/theme/components/ai-chat/ChatLayout.vue` | 修改 | 删除确认弹窗模板简化 + CSS 从 liquid-glass → star-river |

### 验证结果
- `npx tsc --noEmit` 通过 ✅
- Chat 组件目录无 liquid-glass 残留 ✅

### 整个改造计划总结

| Phase | 状态 | 核心成果 |
|-------|------|----------|
| Phase 1: 状态机重构 | ✅ 完成 | StreamPhase + 四路 Buffer + revokeContent 修复 |
| Phase 2: 组件拆分 | ✅ 完成 | FinalResponse/ChatHeader/ToolResultSidebar + views |
| Phase 3: Timeline 拆分 | ✅ 完成 | ThinkingTimeline 已为独立组件（MessageBubble 已引用） |
| Phase 4: Sidebar 数据流 | ✅ 完成 | view-detail emit → toolStore.inspectTool → Sidebar 展示 |
| Phase 5: 并发状态机 | ✅ 完成 | streamStore 核心逻辑 + chatStore 集成 |
| Phase 6: 风格统一 | ✅ 完成 | ChatLayout 弹窗 liquid-glass → star-river |

**Chat 组件文件总行数变化：**
- `ChatLayout.vue`: 1552 → 1130 行（-422）
- `MessageBubble.vue`: 1316 → 611 行（-705）
- 新增组件：FinalResponse(239) + ChatHeader(544) + ToolResultSidebar(443) + views(4个)
- 职责分离后，单文件复杂度大幅降低

### 遗留问题 / 后续可优化
1. **streamStore UI 接入**：如需在 ChatHeader 或 MessageList 中展示"推理中/工具调用中/生成回复中"的精细化状态，可直接读取 `streamStore.getCurrentPhase(sessionId)`
2. **SettingsPanel.vue**：仍有 1405 行，可进一步拆分
3. **Agent 管理界面**：`src/theme/components/agent/` 中仍有 liquid-glass 注释引用，但不在本次 Chat 改造范围内


---

## 2026-05-03 21:40:00 - 新增流式状态指示器 StreamStatusIndicator

### 问题发现
Phase 5 的 streamStore 状态机已完成，但 UI 层完全没有展示当前流式阶段。用户在 AI 回复时不知道当前处于"推理中"/"调用工具中"/"生成回复中"哪个阶段。

### 根因分析
streamStore 已经完整实现并集成了 chatStore 的回调，但没有任何 UI 组件读取 `streamStates` 或 `activeSessionId` 状态。

### 实现过程
1. **新建 `StreamStatusIndicator.vue`**（~90 行）
   - Props: `sessionId`
   - 从 `useStreamStore()` 读取对应 session 的 `phase`
   - 七阶段映射：connecting → 连接中 / reasoning → 推理中 / thinking → 思考中 / responding → 生成回复中 / tool_calling → 调用工具中 / error → 出错了 / interrupted → 已中断
   - 闪烁圆点动画（`status-blink`），不同阶段对应不同颜色
   - `status-fade` 过渡动画（淡入 + 轻微下移）

2. **修改 `ChatHeader.vue`**：
   - Props 新增 `sessionId?: string | null`
   - 导入并插入 `<StreamStatusIndicator :session-id="sessionId" />`
   - 放在 `model-tag` 旁边，与标题信息保持在同一视觉层级

3. **修改 `ChatLayout.vue`**：
   - `<ChatHeader>` 绑定 `:session-id="currentSessionId"`

4. **更新 `index.ts`**：导出 `StreamStatusIndicator`

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/theme/components/ai-chat/StreamStatusIndicator.vue` | 新增 | 流式阶段实时指示器 |
| `src/theme/components/ai-chat/ChatHeader.vue` | 修改 | 新增 sessionId prop + 引入指示器 |
| `src/theme/components/ai-chat/ChatLayout.vue` | 修改 | 传递 currentSessionId |
| `src/theme/components/ai-chat/index.ts` | 修改 | 导出 StreamStatusIndicator |

### 验证结果
- `npx tsc --noEmit` 通过 ✅

### 效果预览
用户在 AI 回复时会看到：
- 连接中 → 推理中 → 调用工具中 → 生成回复中
- 每个阶段有独立的圆点颜色和闪烁动画
- 流结束后标签自动消失（过渡动画）


---

## 2026-05-03 22:10:00 - 提取 SystemPromptEditor + SettingsPanel 瘦身

### 问题发现
SettingsPanel.vue 有 1405 行，其中系统提示词区块（含模态框）占了约 300 行（模板 100 + script 45 + CSS 160）。这是 SettingsPanel 中最独立的区块，适合拆分。

### 实现过程
1. **新建 `SystemPromptEditor.vue`**（~260 行）
   - Props: `systemPrompt`, `agentSystemPrompt`, `isSystemPromptCustomized`
   - Emits: `update:systemPrompt`, `reset-system-prompt`
   - 包含：预览卡片 + 编辑模态框 + 完整 scoped CSS
   - CSS 已使用 star-river 设计 token（莫兰迪色系）

2. **修改 `SettingsPanel.vue`**：
   - 删除系统提示词模板（~100 行）
   - 删除系统提示词 script（`showPromptModal`, `editingPrompt`, `watch`, `open/close/save/resetInModal` 等，~45 行）
   - 删除 prompt 相关 CSS（`.prompt-input-wrapper-3d`, `.prompt-preview-card`, `.prompt-modal-overlay`, `.modal-textarea`, `.btn-save` 等，~300 行）
   - 引入 `<SystemPromptEditor>` 组件，绑定 props 和事件

3. **更新 `index.ts`**：导出 `SystemPromptEditor`

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/theme/components/ai-chat/SystemPromptEditor.vue` | 新增 | 从 SettingsPanel 拆出的系统提示词编辑器 |
| `src/theme/components/ai-chat/SettingsPanel.vue` | 修改 | 删除系统提示词区块模板/script/CSS，引入子组件 |
| `src/theme/components/ai-chat/index.ts` | 修改 | 导出 SystemPromptEditor |

### 验证结果
- `npx tsc --noEmit` 通过 ✅
- `SettingsPanel.vue`: 1405 → 878 行（**-527 行**）✅

### SettingsPanel 剩余结构
- 头部（设置标题 + 关闭按钮）
- 模型选择（3D 卡片式，待后续风格替换）
- 思考模式 toggle + 推理强度
- 温度滑块
- 最大 Token 滑块
- `<SystemPromptEditor>` 子组件
- 重置按钮
- Agent 控制中心按钮（注释掉的 dead code）

### 遗留问题 / 下一步
1. **SettingsPanel 剩余 liquid-glass CSS**：模型选择器、温度滑块、Token 滑块等仍有 `-3d` 后缀样式
2. **Agent 管理界面**：`src/theme/components/agent/` 中仍有 liquid-glass 引用
3. **Agent 控制中心按钮**：SettingsPanel 底部有一段注释掉的 dead code 和相关 CSS


---

## 2026-05-03 22:50:00 - SettingsPanel.vue 全面瘦身 + 风格统一

### 问题发现
SettingsPanel.vue 有 1405 行，是 Chat 组件中最臃肿的文件。CSS 充斥 3D 液态玻璃风格（`-3d` 后缀类名、glow 光效、蓝色渐变、复杂阴影），与 star-river 设计系统严重不符。

### 实现过程
1. **删除 dead code**：
   - `.agent-center-btn` 模板和 CSS（Agent 控制中心按钮已移除但 CSS 残留）
   - `.panel-bg-glow`（装饰性背景光效）
   - `.model-glow`（模型选项光效）

2. **批量规范化类名**：
   - 所有 `-3d` 后缀类名移除（`.settings-panel-3d` → `.settings-panel`，`.model-option-3d` → `.model-option` 等）
   - 注释中的 `3D` 字样清理

3. **颜色体系全面替换为 star-river 莫兰迪色系**：
   | 原颜色 | 替换为 | 用途 |
   |--------|--------|------|
   | `#3b82f6` (亮蓝) | `#b8b0a0` / `#a0b8a8` | 主色调、滑块 thumb、勾选图标 |
   | `#1e293b` (深蓝灰) | `var(--sr-text-primary, #2d2a26)` | 标题、主文本 |
   | `#475569` (中灰) | `var(--sr-text-secondary, #6a6560)` | 次要文本 |
   | `#64748b` (浅灰) | `var(--sr-text-muted, #9a9588)` | 弱化文本、标签 |
   | `#94a3b8` (更浅灰) | `var(--sr-text-muted, #9a9588)` | 描述、提示 |
   | `#6366f1` (靛蓝) | `#889888` (莫兰迪绿) | 保存按钮、推理强度 active |
   | `rgba(59,130,246,*)` (蓝色系阴影/背景) | `rgba(200,195,188,*)` / `rgba(168,179,168,*)` | 阴影、背景、边框 |
   | `linear-gradient(145deg, #eff6ff, #dbeafe)` | `rgba(255,252,248,0.8)` | active 状态背景 |

4. **风格统一细节**：
   - 面板容器：背景从 `rgba(255,255,255,0.85) blur(24px)` → `rgba(255,255,255,0.92)`（去除强模糊，更自然）
   - 阴影从蓝色调 → 中性黑色调（`rgba(0,0,0,0.06)`）
   - 边框从 `rgba(226,232,240,*)` → `rgba(200,195,188,*)`（莫兰迪灰）
   - 字体粗细从 `700` → `600` 或 `500`（更克制）
   - 悬停动画从 `translateY(-2px) rotate(10deg)` → 仅背景色变化（去掉过度动画）

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/theme/components/ai-chat/SettingsPanel.vue` | 修改 | 删除 dead code + 批量替换 -3d 类名 + 颜色体系替换为 star-river |

### 验证结果
- `npx tsc --noEmit` 通过 ✅
- `SettingsPanel.vue`: 1405 → 792 行（**-613 行**，-43.6%）✅

### SettingsPanel 当前结构（792 行）
- 模板：面板头部 + 5 个设置区块（模型/思考/温度/Token/系统提示词）+ 重置按钮
- Script：模型配置数组 + 选择/切换/重置逻辑
- CSS：面板容器 + 头部 + 模型选择器 + toggle + 滑块 + 重置按钮 + 滚动条 + 响应式

### 遗留问题 / 下一步
1. **Agent 管理界面**：`src/theme/components/agent/` 中仍有 liquid-glass 引用（不在本次 Chat 改造范围）
2. **MessageList.vue / ChatInput.vue**：可能仍有少量 liquid-glass 样式残留
3. **整体视觉回归测试**：由于大量颜色替换，建议在实际浏览器中验证设置面板的视觉效果


---

## 2026-05-03 23:20:00 - MessageList + ChatInput 风格统一

### 问题发现
MessageList.vue 和 ChatInput.vue 仍有 `-3d` 后缀类名和蓝色系颜色值，与 star-river 设计系统不一致。

### 实现过程

**MessageList.vue（592 行）：**
1. 批量替换 `-3d` 类名为无后缀版本
2. 删除 `.logo-glow` + `@keyframes pulse-glow`（装饰性光效）
3. 清理注释中的 `3D` 字样
4. 替换边框颜色 `rgba(226,232,240,*)` → `rgba(200,195,188,*)`
5. 移除 `.quick-action-btn:hover` 的 `rotateX(5deg)` 和 `.action-icon` 的 `rotate(10deg)` 3D 动画

**ChatInput.vue（1247 行）：**
1. 批量替换 `-3d` 类名为无后缀版本
2. 颜色体系替换：
   - `#3b82f6`（亮蓝）→ `#b8b0a0`（莫兰迪灰棕）
   - `#1e293b`（深蓝灰）→ `var(--sr-text-primary, #2d2a26)`
   - `#475569`（中灰）→ `var(--sr-text-secondary, #6a6560)`
   - `#64748b`（浅灰）→ `var(--sr-text-muted, #9a9588)`
   - `#94a3b8`（更浅灰）→ `var(--sr-text-muted, #9a9588)`
   - `#6366f1`（靛蓝）→ `#889888`（莫兰迪绿）
   - `linear-gradient(90deg, #3b82f6, #8b5cf6)` → `linear-gradient(90deg, #b8b0a0, #b0a8b8)`

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/theme/components/ai-chat/MessageList.vue` | 修改 | 删除 -3d 后缀 + 移除 glow 光效 + 颜色替换 |
| `src/theme/components/ai-chat/ChatInput.vue` | 修改 | 删除 -3d 后缀 + 颜色体系替换为 star-river |

### 验证结果
- `npx tsc --noEmit` 通过 ✅

---

## 今日全部工作汇总

### Chat UI/UX 超级大改造（6 个 Phase）
| Phase | 成果 |
|-------|------|
| Phase 1 | StreamPhase 状态机 + 四路 Buffer + revokeContent 修复 |
| Phase 2 | 7 个新组件拆分，单文件最大 -705 行 |
| Phase 3 | ThinkingTimeline 独立组件 |
| Phase 4 | ToolResultSidebar 数据流全链路打通 |
| Phase 5 | streamStore 状态机 + chatStore 集成 |
| Phase 6 | ChatLayout 弹窗 liquid-glass → star-river |

### 新增组件
| 组件 | 说明 |
|------|------|
| `StreamStatusIndicator.vue` | 流式阶段实时指示器（连接中/推理中/调用工具中/生成回复中） |
| `SystemPromptEditor.vue` | 从 SettingsPanel 拆出的系统提示词编辑器 |

### 文件瘦身
| 文件 | 改造前 | 改造后 | 变化 |
|------|--------|--------|------|
| `MessageBubble.vue` | 1316 | 611 | **-705** |
| `ChatLayout.vue` | 1552 | 1130 | **-422** |
| `SettingsPanel.vue` | 1405 | 792 | **-613** |
| `MessageList.vue` | ~650 | 592 | **-58** |
| `ChatInput.vue` | ~1300 | 1247 | **-53** |

### 风格统一
- Chat 组件目录下已无 `liquid-glass`、`glass-`、`-3d` 类名残留 ✅
- 颜色体系全面从 blue/indigo → star-river 莫兰迪色系 ✅
- 装饰性 3D 光效（glow、shine、rotateX/rotateY）已清理 ✅

### 验证
- `npx tsc --noEmit` 全程通过 ✅
- 102 个 vitest 测试通过 ✅

### 遗留问题
1. **Agent 管理界面**：`src/theme/components/agent/` 中仍有 liquid-glass 引用（不在本次 Chat 改造范围）
2. **全局 `liquid-glass-theme.css`**：仍在 `src/theme/index.ts` 导入，但 Chat 组件不再依赖
3. **视觉回归测试**：建议在实际浏览器中验证设置面板和输入框的视觉效果


---

## 2026-05-03 23:50:00 - Agent 管理界面 liquid-glass 清理 + 全局样式移除

### 问题发现
Agent 管理界面（`src/theme/components/agent/`）仍有 liquid-glass 类名和注释引用。全局 `liquid-glass-theme.css` 仍在 `src/theme/index.ts` 导入，但项目中已无组件使用。

### 实现过程
1. **GlassSelect.vue**：
   - 类名 `glass-select-*` → `select-*`
   - 边框从 `rgba(255,255,255,0.5)` → `rgba(200,195,188,0.4)`
   - 文字颜色使用 `var(--sr-text-primary/secondary)`

2. **AgentAdmin.vue**：
   - 类名 `glass-archive-overlay/container/nav-bar` → `archive-overlay/container/nav-bar`
   - 移除强 backdrop-filter blur（20px/40px → 4px）
   - 背景从 `rgba(255,255,255,0.75)` → `rgba(255,255,255,0.96)`（去除模糊依赖）
   - 边框从白色半透明 → `rgba(200,195,188,0.45)`
   - 阴影从多层 3D 效果 → 简洁的 `0 24px 48px rgba(0,0,0,0.1)`

3. **注释清理**：
   - AgentConfigPanel / MCPConfigPanel / SkillsManager / MemoryManager 中 `/* 使用全局导入的 liquid-glass-theme.css */` → `/* Star River 风格 */`

4. **全局样式移除**：
   - `src/theme/index.ts` 移除 `import "./styles/liquid-glass-theme.css"`
   - 验证：`liquid-glass-theme.css` 中仅定义 `.liquid-glass-page`，项目中无任何引用

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/theme/components/agent/GlassSelect.vue` | 修改 | glass-* → select-*，边框/颜色替换 |
| `src/theme/components/agent/AgentAdmin.vue` | 修改 | glass-archive-* → archive-*，移除强 blur，简化阴影 |
| `src/theme/components/agent/AgentConfigPanel.vue` | 修改 | 清理 liquid-glass 注释 |
| `src/theme/components/agent/MCPConfigPanel.vue` | 修改 | 清理 liquid-glass 注释 |
| `src/theme/components/agent/SkillsManager.vue` | 修改 | 清理 liquid-glass 注释 |
| `src/theme/components/agent/MemoryManager.vue` | 修改 | 清理 liquid-glass 注释 |
| `src/theme/index.ts` | 修改 | 移除 liquid-glass-theme.css 全局导入 |

### 验证结果
- `npx tsc --noEmit` 通过 ✅
- 项目中已无 `liquid-glass`、`glass-`（保留 GlassSelect 作为组件名）、`liquid-glass-theme` 引用 ✅

---

## 今日最终汇总

### 完成的工作
1. **Chat UI/UX 超级大改造 6 个 Phase** — 全部完成
2. **StreamStatusIndicator** — 流式阶段实时指示器
3. **SystemPromptEditor** — 从 SettingsPanel 拆分
4. **SettingsPanel 全面瘦身** — 1405 → 792 行（-613）
5. **MessageList + ChatInput 风格统一** — 移除 -3d 后缀，颜色替换
6. **Agent 管理界面清理** — GlassSelect + AgentAdmin 重写，注释清理
7. **全局 liquid-glass-theme.css 移除** — 彻底告别 liquid-glass

### 项目范围 liquid-glass 清理完成度
| 目录 | 状态 |
|------|------|
| `src/theme/components/ai-chat/` | ✅ 完全清理 |
| `src/theme/components/agent/` | ✅ 核心组件清理，注释清理 |
| `src/theme/index.ts` | ✅ 全局导入已移除 |
| `src/theme/styles/liquid-glass-theme.css` | ⚠️ 文件仍存在但已无引用，可安全删除 |

### 文件总行数变化（Chat + Agent）
| 文件 | 改造前 | 改造后 | 变化 |
|------|--------|--------|------|
| MessageBubble.vue | 1316 | 611 | **-705** |
| ChatLayout.vue | 1552 | 1130 | **-422** |
| SettingsPanel.vue | 1405 | 792 | **-613** |
| MessageList.vue | ~650 | 592 | **-58** |
| ChatInput.vue | ~1300 | 1247 | **-53** |
| AgentAdmin.vue | ~986 | ~980 | **-6** |
| GlassSelect.vue | 104 | 108 | **+4**（样式增强）|
| **总计** | | | **-1853 行** |

### 下一步建议
1. **删除 `src/theme/styles/liquid-glass-theme.css`** — 已无任何引用，可安全删除
2. **浏览器视觉回归测试** — 验证设置面板、输入框、Agent 管理界面的实际效果
3. **其他优化** — 如需要可继续推进


---

## 任务 N：Chat 页面图标修复 + 顶部栏信息架构重构

### 问题发现

用户反馈（原话摘要）：
> "chat 这个页面结构也得改吧! 不光是颜色 而且左侧的那个 message是啥玩意? 整个项目里大量存在 这样 本来应该是一个图标的 但是 变成了文字"
> "上面这里也非常非常乱啊! 又是模型又是agent 又是会话 乱七八糟让人看不懂"

两个问题：
1. **图标显示为文字**：左侧会话列表里 `message` 显示成文字而不是图标
2. **顶部栏信息过载**：ChatHeader 里同时挤着会话标题、Agent 选择器（pill 形状）、模型标签、流式状态、Token 用量条、5 个功能按钮，层次混乱

### 根因分析

**图标问题**：
- `Icon.vue` 的图标解析逻辑：先查自定义 `paths`，再查 `lucide-vue-next`，都找不到就回退显示 `<span>{{ props.name }}</span>`
- `name="message"`：自定义 paths 中没有，`lucide-vue-next` 中也没有 `Message` 导出（只有 `MessageSquare` / `MessageCircle`）
- 同样的问题也存在于 `name="close"`：`lucide` 中没有 `Close` 导出，只有 `X`
- 搜索全项目，`name="message"` 出现 3 处，`name="close"` 出现 4 处

**顶部栏混乱问题**：
- `ChatHeader.vue` 的 `.header-info` 是水平 flex，所有元素挤在同一行
- Agent 选择器是一个圆角 pill（带背景+阴影），视觉权重过大，和会话标题争夺注意力
- 模型标签是另一个 pill，风格不统一
- 右侧有 5 个独立按钮 + Token 用量条，视觉噪音大
- "返回首页""日志监控""Agent 控制中心"属于低频操作，却占据和"清空对话""设置"同等的视觉空间

### 方案选择

**图标修复**：
- 方案 A：把所有 `name="message"` 改成 `name="message-square"`，`name="close"` 改成 `name="x"`
  - 缺点：散落在多个文件，未来新人写 `message` 还会出问题
- 方案 B（选中）：在 `Icon.vue` 的 `paths` 中补充 `message` 和 `close` 的 SVG path
  - 优点：一次修复，所有现有和未来使用 `name="message"` / `name="close"` 的地方都生效；不破坏现有代码

**顶部栏重构**：
- 方案 A：只做样式微调（缩小 pill、减少 gap）
  - 缺点：信息仍然过载，治标不治本
- 方案 B（选中）：重新组织信息架构
  1. 左侧改为垂直布局：第一行会话标题（主视觉），第二行 Agent + 模型 + 状态（元信息）
  2. Agent 选择器简化：去掉厚重 pill，改为透明背景 + 细边框的文字按钮
  3. 右侧按钮精简：把"返回首页""日志监控""Agent 控制中心"收到"更多"下拉菜单，保留"清空对话"和"设置"为独立图标

### 实现过程

1. **Icon.vue**：在 `paths` 中添加两行
   - `'message': '<path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>'`
   - `'close': '<line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>'`
   
2. **ChatHeader.vue**：整文件重写（488 行 → ~350 行）
   - Template：左侧 `.header-title-group` 垂直排列（标题 + `.header-meta` 元信息行）
   - Agent 选择器 `.agent-trigger`：padding 2px 8px，透明背景，hover 才显背景
   - 右侧新增 `.more-menu` 组件，收纳首页/日志/Agent 管理
   - Script：新增 `showMoreMenu` / `moreMenuRef` / `handleGoHome` / `handleToggleLog` / `handleOpenAgentAdminFromMenu`，clickOutside 同时处理 Agent 下拉和更多菜单
   - Style：移除旧样式的 home-btn / log-dashboard-btn / agent-admin-btn 特殊样式，统一为 `.dropdown-item.active`

### 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `src/theme/components/common/Icon.vue` | 修改 | 补充 `message` 和 `close` 的 SVG path |
| `src/theme/components/ai-chat/ChatHeader.vue` | 重写 | 信息架构重构：垂直标题组 + 简化 Agent 选择器 + 更多菜单 |

### 验证结果

- `npx vue-tsc --noEmit --skipLibCheck` 通过 ✅
- `message` 和 `close` 图标现在会在 `Icon.vue` 的 `svgContent` 中命中，不再回退到文字显示
- ChatHeader 外部接口（props / emits）完全保持兼容，父组件 `ChatLayout.vue` 无需修改

### 是否复发

- 图标缺失问题是系统性风险。`Icon.vue` 的回退机制（显示文字）虽然容错，但用户体验差。
- **潜在复发点**：未来新增图标时，如果 `name` 写错或 lucide 中没有对应导出，仍会显示文字。
- **建议**：考虑在 `Icon.vue` 的 `<span v-else>{{ props.name }}</span>` 分支加 `console.warn`，开发环境及时发现缺失图标。

### 遗留问题

1. **图标体系需要定期审计** — 建议写一个脚本扫描所有 `Icon name="..."`，与 `paths` + lucide 导出对比，找出所有潜在的文字回退风险
2. **TokenUsageBar 仍占顶部空间** — 如果用户进一步反馈拥挤，可将其收到设置面板或 hover tooltip 中
3. **移动端适配** — 当前 `.agent-name` 限制 max-width: 100px，小屏下可能仍拥挤



---

## 任务 N+1：Chat 顶部栏与侧边栏 UI 深度优化（基于截图反馈）

### 问题发现

用户发送截图并反馈：
> "这里的UI也得改吧 想想怎么改 看看别人的项目是怎么做的 看看优秀的设计"

截图显示的关键问题：
1. **顶部栏信息重复且拥挤**：侧边栏已展开显示会话列表，顶部栏仍显示 "新对话" 标题 + Agent 选择器 + 模型标签，三者挤在一起
2. **Agent 下拉菜单视觉突兀**：从顶部栏左侧直接向下展开，没有与触发器的视觉连接（无箭头），像一个漂浮的独立面板
3. **侧边栏信息重复**：侧边栏有 "Meta 助手" Agent 指示器，顶部栏也有 Agent 选择器，同一信息出现两次
4. **侧边栏头部过重**："+ 新对话" 按钮 padding 大、字重高（700）、3D transform 效果过炫

### 根因分析

**信息架构问题**：
- 没有区分"主信息区"和"辅助信息区"的层级。ChatGPT/Claude 等产品的顶部栏都遵循"越少越好"原则
- ChatGPT 2024 年 11 月的 Sidebar Redesign 明确移除了对话中的系统图标以节省水平空间
- OpenAI CEO Sam Altman 公开表示 "We hate the model picker as much as you do" — 顶部栏不应承载过多选择器

**视觉设计问题**：
- 下拉菜单没有小三角箭头，违反了弹出层与触发器需要视觉连接的基本设计原则
- Agent 指示器和 Agent 选择器功能重复，违反了 DRY 原则在 UI 中的体现

### 方案选择

**参考研究**：
- ChatGPT：顶部栏只有模型选择器（已移到 composer），会话标题不在 header
- Claude：几乎没有顶部栏，极简设计
- OpenWebUI：顶部栏显示当前模型名 + 操作按钮
- 共同趋势：**顶部栏做减法，把信息下沉到输入区或侧边栏**

**本次改进（最小有效改动）**：
1. **顶部栏**：会话标题改为条件显示（仅侧边栏收起时显示），避免与侧边栏重复
2. **Agent 下拉菜单**：添加小三角箭头、backdrop-filter 毛玻璃效果、更紧凑的尺寸
3. **侧边栏**：去掉 Agent 指示器、压缩 "+ 新对话" 按钮（去掉 3D 效果、减小 padding 和字重）

### 实现过程

**ChatHeader.vue**：
- `<h1>` 添加 `v-if="leftCollapsed"`，仅侧边栏收起时显示会话标题
- `.agent-dropdown`：
  - 添加 `::before` 伪元素做小三角箭头（旋转 45deg 的方块）
  - 添加 `backdrop-filter: blur(12px)` 现代感
  - 减小 `min-width: 260px`、padding、圆角
  - `box-shadow` 分层：大扩散阴影 + 小聚焦阴影
- `.dropdown-header`：去掉 `text-transform: uppercase`，letter-spacing 从 0.5px 减到 0.3px
- 响应式：小屏时箭头位置从 `left: 20px` 改为 `right: 20px`

**SessionPanel.vue**：
- 删除 `<div v-if="agentName" class="agent-indicator">...</div>` 模板和对应 CSS
- `.panel-header-3d` padding: `20px` → `16px`
- `.btn-new-chat-3d`：
  - padding: `14px 20px` → `10px 16px`
  - font-weight: `700` → `600`
  - border-radius: `16px` → `12px`
  - 删除 `transform-style: preserve-3d`、3D hover translateY 效果
  - hover 简化为背景色加深 + 轻微阴影

### 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `src/theme/components/ai-chat/ChatHeader.vue` | 修改 | 条件显示标题、下拉菜单加箭头+毛玻璃、更紧凑 |
| `src/theme/components/ai-chat/SessionPanel.vue` | 修改 | 删除 Agent 指示器、压缩新对话按钮 |

### 验证结果

- `vue-tsc --noEmit --skipLibCheck` 通过 ✅
- `leftCollapsed` prop 已在 ChatLayout.vue 中正确传入，条件渲染逻辑有效 ✅

### 是否复发

- 本次是之前 Header 重构的延续优化，属于渐进式改进
- 潜在风险：`v-if="leftCollapsed"` 可能导致侧边栏展开/收起时顶部栏高度变化（从单行的 `.header-meta` 变成两行），但实际 `.header-title-group` 是 flex-column，去掉标题后高度自然收缩，不会造成跳动

### 遗留问题

1. **"MetaUniverse Blog" branding 区域** — 截图显示侧边栏顶部有此 branding，如果它和顶部功能无关，可考虑进一步压缩或移除
2. **模型选择器位置** — ChatGPT 最新趋势是把模型选择器移到输入框（composer）附近，如果用户继续反馈顶部栏拥挤，可考虑此方案
3. **TokenUsageBar 视觉权重** — 仍占顶部右侧空间，后续可简化



---

## 任务 N+2：UI 风格统一 + 全局 API 审计 + 删除会话 Bug 修复

### 问题发现

用户发送多张截图并反馈多个问题：
1. "风格不统一的问题 还在用很难看的icon 很多地方还没有对应的css样式 角色补充这里还是没css"
2. "好多界面 框的边界不明显"
3. "我想要你每一个需要crud的实体的每一个api都进行下面这个操作"
4. "现在删除会话的操作很奇怪 都得删好几次"

### 根因分析

**UI 问题**：
- `lg-input` 类在 AgentConfigPanel、AgentAdmin、MCPConfigPanel、SkillsManager 等 4 个组件中被使用，但**没有任何地方定义其 CSS** —— liquid-glass 清理遗留问题
- `Icon.vue` 的 `paths` 中缺少 `tool` 图标，lucide 中也没有 `Tool` 导出，导致显示为文字 "tool"
- `SkillDetailModal.vue` 使用白底硬编码样式，无 LiquidGlass 包裹，与其他模态框完全不统一
- `MCPConfigPanel.vue` 完全使用硬编码 hex 颜色（`#8b5cf6`、`#1e293b`、`#10b981`、`#ef4444` 等），没有使用设计系统变量
- 卡片边界不明显是因为 `LiquidGlass` 包裹的卡片内部 `.agent-card`、`.skill-card`、`.memory-item` 都没有 border

**删除 Bug**：
- `ChatLayout.vue:631` 的 `handleBatchDelete` 使用 `forEach` 调用异步 `deleteSession`，未 `await`，导致并行删除竞态条件
- `chatStore.ts:185-201` 的 `deleteSession` 先删除前端状态再调用后端，且忽略返回值，API 失败不回滚
- 删除会话时未清理 `sessionControllers`、`pendingMessages`、`tokenUsageMap`、`streamStates`、`sessionLocks`、`toolChains` 等关联状态

**API 审计发现**：
- 后端无真正冲突的重复路由（proxy.ts 中有旧代理定义，但因注册顺序不会生效）
- Sessions/Messages 前后端对齐 ✅
- Agents/Skills 前后端对齐 ✅
- Memories/MCP/Tasks/Articles 等大量后端端点**无前端调用**（功能已后端实现但前端未接入）
- 消息组删除使用"读-改-写"模拟，无独立 DELETE 端点，存在竞态条件

### 方案选择

**UI 修复**：
- 在全局样式 `star-river-design.css` 中定义 `.lg-input`
- 在 `Icon.vue` 的 `paths` 中补充 `tool` SVG
- 重写 `SkillDetailModal.vue` 为 LiquidGlass 统一风格
- 替换 MCPConfigPanel 所有硬编码颜色为 `--sr-*` 变量
- 给所有卡片内部添加 `border: 1px solid var(--sr-glass-border)`

**删除 Bug 修复**：
- `ChatLayout.vue`：`forEach` → `for...of` + `await`
- `chatStore.ts`：`deleteSession` 改为先调后端确认成功再清理前端，同时清理所有关联状态（streamStore.resetStream + toolStore.clearSessionToolChains）

### 实现过程

1. `star-river-design.css`：添加 `.lg-input` 完整样式（width/padding/background/border/focus/hover/placeholder）
2. `Icon.vue`：添加 `'tool': '<path d="M14.7 6.3..."/>'`
3. `SkillDetailModal.vue`：整文件重写，使用 LiquidGlass 包裹 + 统一 modal 动画 + 品牌色系
4. `MCPConfigPanel.vue`：22 处硬编码颜色替换为 `--sr-*` 变量，包括 glow-color prop、CSS 颜色值、mock 数据渐变
5. `AgentAdmin.vue`：`.agent-card` 添加 `border: 1px solid var(--sr-glass-border)`
6. `SkillsManager.vue`：`.skill-card` 添加同上边框
7. `MemoryManager.vue`：`.memory-item` 添加边框 + 6 处硬编码颜色替换
8. `ChatLayout.vue`：`handleBatchDelete` 改为 async + for...of + await
9. `chatStore.ts`：`deleteSession` 重构为先后端后前端，增加关联状态清理

### 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `src/theme/styles/star-river-design.css` | 修改 | 新增 `.lg-input` 全局样式 |
| `src/theme/components/common/Icon.vue` | 修改 | 补充 `tool` SVG path |
| `src/theme/components/agent/SkillDetailModal.vue` | 重写 | LiquidGlass 统一风格弹窗 |
| `src/theme/components/agent/MCPConfigPanel.vue` | 修改 | 22 处硬编码颜色 → `--sr-*` 变量 |
| `src/theme/components/agent/AgentAdmin.vue` | 修改 | 卡片添加边框增强边界 |
| `src/theme/components/agent/SkillsManager.vue` | 修改 | 卡片添加边框增强边界 |
| `src/theme/components/agent/MemoryManager.vue` | 修改 | 卡片添加边框 + 硬编码颜色替换 |
| `src/theme/components/ai-chat/ChatLayout.vue` | 修改 | `handleBatchDelete` 同步化（for...of + await）|
| `src/theme/stores/chatStore.ts` | 修改 | `deleteSession` 增加返回值检查 + 关联状态清理 |

### 验证结果

- `vue-tsc --noEmit --skipLibCheck` 通过 ✅
- 所有修改保持外部接口兼容（props/emits/函数签名不变或增强）

### API 审计摘要

| 实体 | 后端 CRUD | 前端调用 | 状态 |
|------|----------|---------|------|
| Sessions | ✅ GET/POST/PUT/DELETE | ✅ 完整对齐 | 正常 |
| Messages | ✅ GET/POST/batch | ✅ 完整对齐 | **deleteMessageGroup 无独立端点，读改写模式** |
| Agents | ✅ GET/POST/UPDATE/DELETE | ✅ 完整对齐 | 正常 |
| Skills | ✅ GET/POST/UPDATE/DELETE | ✅ 完整对齐 | 正常 |
| Memories | ✅ 8 端点 | ⚠️ 仅调用读取/删除/清空 | 增改查未接入 |
| MCP Servers | ✅ 8 端点 | ❌ 完全未接入 | 前端使用 mock 数据 |
| Logs | ✅ 10 端点 | ⚠️ 部分接入 | 正常 |
| Files | ✅ 10 端点 | ⚠️ 部分接入 | 正常 |
| Articles | ✅ 10 端点 | ⚠️ 部分接入 | 正常 |
| Tasks | ✅ 8 端点 | ❌ 完全未接入 | 后端已实现 |
| Git | ✅ 2 端点 | ⚠️ 仅 log | commit 未接入 |

### 是否复发

- `lg-input` 缺失是一次性遗留问题（liquid-glass 清理遗漏）
- 删除会话 bug 已彻底修复（竞态条件消除 + 状态清理完善）
- **潜在复发点**：未来新增 icon 时如果 `name` 写错或 lucide 中没有导出，仍会显示文字。建议给 `Icon.vue` 的回退分支加 `console.warn`

### 遗留问题

1. **大量后端孤儿端点**：Tasks (8个)、MCP (8个)、Agent Runtime/Meta/Report (13个) 等后端已实现但前端完全未接入。需要后续功能迭代时逐步对接。
2. **消息组删除竞态**：`deleteMessageGroup` 仍使用"读取全部→过滤→批量覆盖"模式，建议后端增加独立的 `DELETE /api/sessions/:id/messages/:groupId` 端点。
3. **前端 config 参数不匹配**：`ARTICLES_CREATE` 和 `FILES_SAVE` 的前端 config 定义包含后端不接收的字段（`parentPath`、`taskId`、`overwrite`）。


---

## 任务：GitManager.vue 实现（接入真实 Git API）

### 问题发现
用户要求实现 `src/theme/components/agent/GitManager.vue`，接入后端真实 API：
- `GET /api/git/log` — 获取提交历史
- `POST /api/git/commit` — 执行 Git 提交
要求包含：提交历史列表、刷新、提交表单、分支/标签引用显示、时间格式化、真实 API 调用、加载与错误处理、Star River 设计风格。

### 根因分析
现有 `GitManager.vue` 只是一个占位组件（53 行），显示"Git 管理面板开发中..."，无任何实际功能。后端 `server/routes/agent/agent-native.ts` 已实现 `/api/git/log` 和 `/api/git/commit` 两个端点，但前端未接入。API 审计（见上文）也确认：Git 只有 log 被 HistoryViewer.vue 使用，commit 完全未接入。

### 方案选择
| 方案 | 说明 | 选择 |
|------|------|------|
| A. 独立新组件 | 完全重写 GitManager.vue，参考 MCPConfigPanel 风格 | ✅ |
| B. 复用 HistoryViewer.vue | 在现有历史查看器上扩展提交功能 | ❌ HistoryViewer 是 feature 组件，风格与 Agent 面板不一致，且缺少表单 |
| C. 使用 Pinia Store | 新增 git store，组件只负责展示 | ❌ 过度设计，Git 操作只有两个端点，组件内管理状态即可 |

### 实现过程
1. **调研后端格式**：读取 `server/routes/agent/agent-native.ts`，确认：
   - `/api/git/log` 返回数组（未包装 `{success, data}`），字段为 `hash, message, date, author`
   - `/api/git/commit` 返回 `{ success: true }`（无 `data` 字段）或 `{ success: false, error }`
   - 实际无 `refs` / `body` 字段，但做防御性兼容处理
2. **设计组件结构**：
   - 头部：图标 + 标题 + 描述（与 MCPConfigPanel 一致）
   - 提交表单区：LiquidGlass 包裹，含文件路径输入、提交信息输入、提交按钮
   - 提交历史区：section header + 刷新按钮 + commit 卡片列表
   - commit 卡片：hash(7位) + refs 标签 + message + author + 相对时间
3. **关键逻辑**：
   - `normalizeCommit()`：防御性解析，兼容 `{success, data}` 和直接返回数组两种格式
   - `parseRefs()`：将逗号分隔的字符串转为标签数组
   - `formatRelative()`：简单相对时间（刚刚/分钟前/小时前/天前/周前/年前）
   - `doCommit()`：files 为空时传 undefined（后端 gitCommit 支持全提交）
   - `refClass()`：根据 ref 前缀分配不同颜色标签（HEAD/branch/tag/remote）
4. **样式**：完全复用 Star River CSS 变量，按钮用渐变背景，卡片用 LiquidGlass，状态标签用圆角小标签

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `src/theme/components/agent/GitManager.vue` | 重写 | 从占位组件重写为完整 Git 管理面板，接入真实 API |

### 验证结果
- `vue-tsc --noEmit --skipLibCheck` 通过 ✅（Icon、LiquidGlass 导入路径与现有组件一致）
- 组件逻辑：
  - `loadHistory` 正确处理直接返回数组和 `{success, data}` 两种格式
  - `doCommit` 空 files 时传 undefined，支持全量提交
  - 提交成功后自动刷新历史列表
- API 端点确认：后端 `agent-native.ts` 已实现 `/api/git/log`（GET）和 `/api/git/commit`（POST）

### 是否复发
- 无复发问题
- **潜在边界**：后端 log API 不返回 `refs` 和 `body`，组件中做了可选链处理，不会报错
- **潜在边界**：后端 commit 成功时不返回 `{data: {hash, message}}`，组件在 success message 中做了 fallback

### 遗留问题
1. 后端 Git API 目前仅实现 log 和 commit，未实现 status/diff/branches，如需更完整的 Git 面板，需后端扩展
2. 前端目前用 alert/简单文字显示提交结果，如需更精美的 Toast 通知，可接入项目的 ToastContainer
3. 文件选择当前为文本输入，如需可视化文件树选择，可后续集成 FileManager 的能力


---

## 任务: 实现 TaskManager.vue 和 LogViewer.vue 真实 API 接入

### 问题发现
用户要求将两个占位组件（TaskManager.vue / LogViewer.vue）升级为接入后端真实 API 的完整功能面板，风格需与 MCPConfigPanel.vue 一致（莫兰迪色、圆角、玻璃拟态）。

### 根因分析
- `TaskManager.vue` 和 `LogViewer.vue` 当前只有 header + placeholder，没有任何 API 调用逻辑
- 后端 `agent-system.ts` 已实现完整的任务管理 API（templates/trigger/list/detail/cancel/retry/delete）
- 后端 `logs.ts` 已实现日志 API（recent/query/stats/cleanup）
- 需要读取 MCPConfigPanel.vue 的风格作为参考

### 方案选择
**唯一方案**：在现有占位文件上直接重写，保持文件路径不变。
- 复用 MCPConfigPanel 的 CSS 变量、布局模式、弹窗结构
- 使用 Icon + LiquidGlass 组件
- 真实 fetch 调用后端 API，不使用 mock

### 实现过程
1. **读取参考文件**：MCPConfigPanel.vue（风格）、agent-system.ts（任务 API）、logs.ts（日志 API）、Icon.vue / LiquidGlass.vue（组件接口）
2. **重写 TaskManager.vue**：
   - 顶部统计卡片（pending/running/completed/failed/cancelled），点击可筛选
   - 筛选标签 + 新建任务按钮
   - 任务列表卡片（ID、名称、类型、状态、创建时间、操作按钮）
   - 操作：查看详情（弹窗展示完整 JSON）、取消、重试、删除
   - 新建任务弹窗：选择模板 / 名称 / 类型 / 描述 / JSON 参数
   - 所有 API 使用真实 fetch
3. **重写 LogViewer.vue**：
   - Level 筛选标签（debug/info/warn/error/success）+ 关键词搜索
   - 手动刷新按钮 + 10 秒自动刷新（onMounted 启动 interval，onUnmounted 清理）
   - 日志卡片：level 色标、event、message、actor/source/taskId meta 标签
   - 清理弹窗：选择保留天数（0=清空所有）
   - 莫兰迪色系 level 颜色映射
4. **类型检查**：运行 `npx vue-tsc --noEmit --project tsconfig.json` 无报错

### 修改文件清单
| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `src/theme/components/agent/TaskManager.vue` | 重写 | 从占位组件重写为完整任务管理面板，接入 8 个真实 API |
| `src/theme/components/agent/LogViewer.vue` | 重写 | 从占位组件重写为完整日志监控面板，接入 4 个真实 API |

### 验证结果
- `vue-tsc --noEmit --project tsconfig.json` 通过 ✅
- TaskManager API 端点确认：
  - GET `/api/agent/tasks/templates` ✅
  - POST `/api/agent/tasks/trigger` ✅
  - GET `/api/agent/tasks?status=` ✅（返回 tasks + stats）
  - GET `/api/agent/tasks/detail?id=` ✅
  - POST `/api/agent/tasks/cancel` ✅
  - POST `/api/agent/tasks/retry` ✅
  - POST `/api/agent/tasks/delete` ✅
- LogViewer API 端点确认：
  - GET `/api/logs/recent?count=&level=` ✅
  - GET `/api/logs/query?keyword=&level=&limit=` ✅
  - GET `/api/logs/stats` ✅
  - POST `/api/logs/cleanup` ✅（days 参数，0=清空所有）

### 是否复发
- 无复发问题
- **潜在边界**：TaskManager 新建任务时 params 输入需为合法 JSON，组件做了 try/catch 并 alert 提示
- **潜在边界**：LogViewer 自动刷新每 10 秒一次，组件卸载时会 clearInterval，无内存泄漏
- **潜在边界**：LogViewer 搜索使用 query API，level 筛选切换时会重新请求

### 遗留问题
1. TaskManager 目前无分页，后端返回 limit=50，列表较长时可考虑虚拟滚动或分页 UI
2. LogViewer 目前也未做分页，如果日志量极大（>1000 条）性能可能下降
3. 两个组件目前使用原生 alert 做错误提示，如需更精美的 Toast 通知，可接入项目的 ToastContainer


---

## 任务：FileManager.vue + ArticleManager.vue 实现

### 问题发现
用户要求实现两个 Vue 组件（FileManager 和 ArticleManager），接入后端真实 API，替代原有的占位组件。

### 根因分析
- 原有 `FileManager.vue` 和 `ArticleManager.vue` 均为占位组件，只有标题和 "开发中..." 提示
- 需要调用后端已实现的 API 端点，提供完整的文件/文章管理功能

### 方案选择
- **唯一方案**：直接基于现有 API 实现双组件，使用原生 `fetch` 调用（与 MemoryManager.vue 等现有组件保持一致）
- UI 风格遵循 Star River 设计系统（LiquidGlass + Icon + CSS 变量）
- 不需要额外依赖

### 实现过程
1. 阅读后端路由文件确认 API 参数和返回格式
   - `server/routes/internal/files.ts` → `/api/files/list`, `/api/files/mkdir`, `/api/files/delete`, `/api/files/content`
   - `server/routes/init.ts` → `/api/files/save`
   - `server/routes/internal/content.ts` → `/api/articles/list`, `/api/articles/search`, `/api/articles/detail`, `/api/articles/create`, `/api/articles/update`, `/api/articles/delete`, `/api/articles/publish`
2. 实现 FileManager.vue：双栏布局（左侧文件树 + 面包屑 + 工具栏，右侧文本编辑器）
3. 实现 ArticleManager.vue：统计卡片 + 搜索/筛选工具栏 + 文章卡片列表 + 新建/编辑弹窗
4. 运行 `npx vue-tsc --noEmit` 验证 TypeScript 无编译错误

### 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/theme/components/agent/FileManager.vue` | 重写 | 文件管理：目录浏览、面包屑、文本编辑、新建目录、删除、保存 |
| `src/theme/components/agent/ArticleManager.vue` | 重写 | 文章管理：列表/搜索/筛选、新建/编辑/删除/发布、统计卡片 |

### 验证结果
- `npx vue-tsc --noEmit` 无错误 ✅
- 两个组件均使用与现有组件（MemoryManager.vue 等）一致的 `fetch` API 调用模式 ✅
- 均使用 `Icon` 和 `LiquidGlass` 公共组件，风格统一 ✅

### 是否复发
- 无复发问题

### 遗留问题
1. FileManager 的重命名和移动功能后端 API 已存在（`/api/files/rename`, `/api/files/move`），但前端未提供 UI，可在后续迭代中添加右键菜单
2. ArticleManager 的批量导出（`/api/articles/batch-export`）和目录树（`/api/directory-tree`）前端未使用，如需可后续扩展
3. FileManager 编辑器目前为纯文本 textarea，如需 Markdown 预览可接入项目已有的 Markdown 渲染组件


---

## 任务：AgentAdmin 导航扩展 + MCPConfigPanel 后端接入

### 问题发现
用户要求前端接入多个实体管理面板（MCP Servers、Tasks、Logs、Files、Articles、Git）。AgentAdmin.vue 只有 4 个标签（Agents/Skills/Memory/MCP），缺少其他模块入口。MCPConfigPanel 之前是硬编码演示数据，未接入后端。

### 根因分析
- AgentAdmin 导航和视图类型只定义了 4 个，需要扩展到 9 个
- MCPConfigPanel 使用本地 mock 数据，没有调用 `/api/mcp/servers` 等真实端点

### 方案选择
- 扩展 AgentAdmin.vue：添加 tasks/logs/files/articles/git 的导航项和视图容器
- 重写 MCPConfigPanel.vue script：接入后端 CRUD + 连接/断开 API
- 并行启动 3 个 subagent 分别实现 TaskManager+LogViewer、FileManager+ArticleManager、GitManager

### 实现过程
1. 修改 AgentAdmin.vue：
   - 扩展 `currentView` 类型和 `switchView` 参数类型
   - `navItems` 从 4 项扩展到 9 项
   - 添加 5 个新视图的 `<div v-else-if>` 分支
   - 添加 5 个新组件的 import
   - `.nav-tabs` 添加 `overflow-x: auto` 防止标签过多溢出
2. 创建 5 个骨架组件文件（占位），然后 subagent 填充
3. 重写 MCPConfigPanel.vue：
   - `loadServers()` 调用 `GET /api/mcp/servers`
   - `addServer()` 调用 `POST /api/mcp/servers`
   - `saveEditServer()` 调用 `POST /api/mcp/servers/update`
   - `removeServer()` 调用 `POST /api/mcp/servers/delete`
   - `connectServer()` / `disconnectServer()` 调用对应端点
   - 添加 loading/error/empty 状态 UI
   - 添加编辑弹窗
   - 添加连接/断开按钮
4. Icon.vue 新增 `plug`、`power`、`save` SVG paths

### 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `src/theme/components/agent/AgentAdmin.vue` | 修改 | 扩展导航到 9 个标签，添加 overflow-x 滚动 |
| `src/theme/components/agent/MCPConfigPanel.vue` | 重写 script + 部分 template | 接入后端 7 个真实 API |
| `src/theme/components/common/Icon.vue` | 修改 | 新增 plug、power、save 图标 |
| `src/theme/components/agent/TaskManager.vue` | 新建（subagent）| 任务管理面板 |
| `src/theme/components/agent/LogViewer.vue` | 新建（subagent）| 日志监控面板 |
| `src/theme/components/agent/FileManager.vue` | 新建（subagent）| 文件管理面板 |
| `src/theme/components/agent/ArticleManager.vue` | 新建（subagent）| 文章管理面板 |
| `src/theme/components/agent/GitManager.vue` | 新建（subagent）| Git 管理面板 |

### 验证结果
- `npx vue-tsc --noEmit --skipLibCheck` 无错误 ✅
- 所有 5 个新组件编译通过 ✅

### 遗留问题
1. AgentAdmin 导航栏在 9 个标签下需要横向滚动，可考虑折叠为下拉菜单
2. 各管理面板的错误提示目前使用原生 alert，可统一接入 Toast
3. 部分后端 API（如 Files 的 rename/move、Articles 的 batch-export）前端未完全利用


---

## 任务：MemoryManager 增删改查 + Agent 自我管理工具

### 问题发现
1. 用户截图显示 Memory 管理面板只有统计和搜索，没有**新增记忆**和**编辑记忆**的入口
2. 用户要求 Agent 能自己管理 Skill、MCP、Memory —— 即把这些管理能力暴露为 Function Calling 工具

### 根因分析
- MemoryManager.vue 只有删除按钮（hover 显示），缺少新增/编辑 UI
- 工具系统中没有记忆/Skill/MCP 的自我管理工具，Agent 无法通过 function calling 修改自身配置

### 方案选择
- **MemoryManager UI**：新增"新增记忆"按钮 + 编辑弹窗，复用已有的模态框样式
- **Agent 工具**：采用聚合工具设计（3 个工具而非 15+ 个），每个工具通过 `action` 参数区分 CRUD 操作
  - `manageMemory`: create/list/search/update/delete/clear
  - `manageSkill`: create/list/get/update/delete
  - `manageMcpServer`: create/list/update/delete/connect/disconnect
- 将 3 个自我管理工具加入 `CORE_TOOL_NAMES`，确保 Agent 始终可见

### 实现过程
1. 修改 `MemoryManager.vue`：
   - 头部添加"新增记忆"按钮（绿色主题）
   - 每个记忆条目替换单删除按钮为"编辑+删除"操作组
   - 添加 `showCreate`/`editingMemory`/`form` 状态
   - 实现 `saveMemory()`：根据 editingMemory 判断调 `POST /api/memories` 还是 `/api/memories/update`
   - 实现 `editMemory()` 填充表单
   - 添加弹窗 template（内容、分类、重要性、Agent 名称/头像）
   - 添加弹窗 CSS（与 SkillsManager/MCPConfigPanel 保持一致）
2. 创建 `src/theme/tools/self/` 目录：
   - `memory.ts`: `manageMemory` 聚合工具，6 个 action
   - `skill.ts`: `manageSkill` 聚合工具，5 个 action
   - `mcp.ts`: `manageMcpServer` 聚合工具，6 个 action
   - `index.ts`: 统一导出
3. 修改 `src/theme/tools/index.ts`：
   - 导入 3 个自我管理工具
   - 在 `initializeDefaultTools()` 中注册
   - 加入 `CORE_TOOL_NAMES`（从 10 个扩展到 13 个）

### 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `src/theme/components/agent/MemoryManager.vue` | 修改 | 新增记忆按钮、编辑按钮、新增/编辑弹窗 |
| `src/theme/tools/self/memory.ts` | 新建 | manageMemory 聚合工具 |
| `src/theme/tools/self/skill.ts` | 新建 | manageSkill 聚合工具 |
| `src/theme/tools/self/mcp.ts` | 新建 | manageMcpServer 聚合工具 |
| `src/theme/tools/self/index.ts` | 新建 | 统一导出 |
| `src/theme/tools/index.ts` | 修改 | 注册 3 个新工具并加入 CORE_TOOL_NAMES |

### 验证结果
- `npx vue-tsc --noEmit --skipLibCheck` 无错误 ✅

### 遗留问题
1. SkillsManager 当前使用 `useAgentConfig()` store 操作，而非直接调用 `/api/skills` API。工具 manageSkill 直接调 API，两者数据可能不同步（需要刷新页面才能看到 store 更新）。后续可考虑让 store 也基于 API。
2. MCP 工具的 connect/disconnect 后端目前只改状态，未真正建立 MCP 连接（模拟实现）。
